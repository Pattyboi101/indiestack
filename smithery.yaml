# Smithery configuration: https://smithery.ai/docs/deployments
startCommand:
  type: stdio

configSchema:
  type: object
  properties: {}

commandFunction:
  |-
  (config) => ({ command: 'uvx', args: ['--from', 'indiestack', 'indiestack-mcp'] })
