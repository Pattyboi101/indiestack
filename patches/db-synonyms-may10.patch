diff --git a/src/indiestack/db.py b/src/indiestack/db.py
index 36555b0..976fb0f 100644
--- a/src/indiestack/db.py
+++ b/src/indiestack/db.py
@@ -2604,6 +2604,14 @@ _CAT_SYNONYMS: dict[str, str] = {
     "rag": "ai",
     "embedding": "ai",
     "embeddings": "ai",
+    # LLM paradigm terms — bigrams override single-token collisions
+    "calling": "ai",                # "tool calling api" — "tool" is stop word, "calling" lands first
+    "function calling": "ai",       # bigram — "function calling" as term-of-art for LLM tool use
+    "ai proxy": "ai",               # bigram overrides "proxy"→devops — LiteLLM/Portkey are AI, not DevOps
+    "token limit": "ai",            # bigram overrides "token"→authentication — LLM token window queries
+    "token pricing": "ai",          # bigram overrides "token"→authentication — LLM cost queries
+    "document qa": "ai",            # bigram overrides "document"→database — RAG/LLM document Q&A
+    "document q&a": "ai",           # ampersand variant — "document q&a tool" → AI & Automation
     # Vector databases (stored under database category)
     "vector": "database",
     # Direct category name fragments (for non-first-position terms like "self hosted auth")
@@ -6613,8 +6621,7 @@ _CAT_SYNONYMS: dict[str, str] = {
     "load balancer": "devops",     # "load balancer setup", "load balancer alternative" → DevOps & Infrastructure
     "load balancing": "devops",    # "load balancing tool", "load balancing software" → DevOps & Infrastructure
     "load-balancing": "devops",    # hyphenated — "load-balancing solution", "load-balancing nginx" → DevOps
-    # Search — full text search multi-word (no hyphen/compound) form
-    "full text search": "search",  # "full text search engine", "full text search library" → Search Engines
+    # Search — "full text search" routes via "search" single token (3-token key can never fire)
     # Testing — hyphenated form of typecheck (compound forms mapped; dashed form wasn't)
     "type-check": "testing",       # "type-check script", "type-check ci", "type-check alternative" → Testing Tools
     # Frontend — code-splitting hyphenated form (complement to "splitting"→frontend already mapped)
