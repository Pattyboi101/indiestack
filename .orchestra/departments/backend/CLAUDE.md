# Backend Department

You are the Backend department agent for IndieStack. You handle database logic, auth, payments, and server-side processing.

## CRITICAL: aiosqlite Row Access
aiosqlite with row_factory=Row uses DICT access: row["column_name"], NOT row[0].
ALWAYS use column name aliases in SQL (SELECT COUNT(*) as n) and access via row["n"].
This has caused production bugs TWICE. Never use integer indexing on query results.

## Your Scope
- `src/indiestack/db.py` — SQLite with aiosqlite, WAL mode
- `src/indiestack/auth.py` — GitHub OAuth, sessions
- `src/indiestack/payments.py` — Stripe subscriptions
- `src/indiestack/main.py` — FastAPI app, middleware, router imports
- `src/indiestack/config.py` — configuration
- `src/indiestack/email.py` — Gmail SMTP
- `scripts/` — data processing scripts

## Rules
- Use `request.state.user` for auth (populated by middleware). Never query users by session_token.
- Use `d = request.state.db` to avoid shadowing db module import.
- `category_slug` is on `categories` table, not `tools` — use JOIN.
- When changing shared DB function return shapes, grep ALL callers across ALL route files.
- ALTER TABLE ADD COLUMN can't include UNIQUE — add column first, then CREATE UNIQUE INDEX.
- Use `python3` not `python`.
- When adding a new category to the DB, also add a matching entry to `NEED_MAPPINGS` in db.py (drives Stack Builder + Use Cases pages) and add relevant terms to `_CAT_SYNONYMS` for search routing.
- CRITICAL: When adding `_CAT_SYNONYMS` entries targeting a new category, verify the category exists in `init_db()`. Grep `grep "INSERT INTO categories" db.py` to check. If missing, add it to the v3 migration block. The `ai-standards` category was missing from init_db() for weeks despite having ~50 `_CAT_SYNONYMS` entries — fixed May 2026.
- `_CAT_SYNONYMS` uses short-name values — NOT category slugs. Full mapping (category-slug → synonym-value):
  authentication→"authentication", payments→"payments", analytics-metrics→"analytics", email-marketing→"email", invoicing-billing→"invoicing", monitoring-uptime→"monitoring", forms-surveys→"forms", scheduling-booking→"scheduling", headless-cms→"cms", customer-support live-chat→"customer" / helpdesk→"support", seo-tools→"seo", file-management→"file", crm-sales→"crm", developer-tools→"developer", ai-automation→"ai", design-creative→"design", feedback-reviews→"feedback", social-media→"social", project-management→"project", landing-pages→"landing", api-tools→"api", devops-infrastructure→"devops", frontend-frameworks→"frontend", caching→"caching", mcp-servers→"mcp", boilerplates→"boilerplate", feature-flags→"feature", background-jobs→"background", database→"database", testing-tools→"testing", security-tools→"security", search-engine→"search", message-queue→"message", media-server→"media", maps-location→"maps", logging→"logging", notifications→"notifications", localization→"localization", cli-tools→"cli", documentation→"documentation", newsletters-content→"newsletters", learning-education→"learning", creative-tools→"creative", games-entertainment→"games", ai-dev-tools→"ai dev" (NOTE: must be "ai dev" with a space — LOWER('AI Dev Tools') LIKE '%ai dev%' → TRUE; "aidev" without space does NOT match), ai-standards→"ai standards" (must be "ai standards" with space — same reason as ai dev)
- When adding `_CAT_SYNONYMS` entries, always add BOTH hyphenated and non-hyphenated variants for compound terms (e.g. "autoscaling" AND "auto-scaling", "keyvalue" AND "key-value"). Check with `grep '"term"' db.py` before adding to avoid Python's silent duplicate-key override.
- When adding a new popular tool to `_CAT_SYNONYMS`, also add TLD-variant forms (.com, .io, .app, .so, .tech) e.g. `"supabase.com": "database"` alongside `"supabase": "database"`. Users frequently paste domain names into search queries ("supabase.com alternative"). Probe pattern 43 (May 2026) fixed this for 19 tools.
- Probe pattern 44 (May 2026): container/docker security collisions + threat dead zones + supply-chain spaced form + SaaS billing collision. "container scanning/security/vulnerability" and "docker security/vulnerability" routed to devops via "container"→devops (wrong; Trivy/Grype are Security). "supply chain" spaced bigram was missing (only hyphenated/compound forms existed). "threat" bare token had no mapping → "threat detection/modeling" fired raw_first. "key management" fired "management"→project (wrong; KMS tools are Security). "saas billing/payments/subscription" fired "saas"→boilerplate (wrong; should be Payments). Fixed: bigrams "container scanning/security/vulnerability"→security, "docker security/vulnerability"→security, "supply chain"→security, bare "threat"→security, bigram "key management"→security, bigrams "saas billing/payments/subscription"→payments. Now 1005/1005 routing tests pass.
- Probe pattern 45 (May 2026): workflow-automation collisions + LLM monitoring dead zones + SBOM routing + mobile analytics collision. "workflow builder"/"workflow automation" mis-routed to ai via "workflow"→ai (n8n, Make.com, Activepieces, Temporal belong in Background Jobs). "visual workflow builder" mis-routed to testing via "visual"→testing. "llm monitoring"/"llm observability" mis-routed to ai — Langfuse, Helicone, Arize Phoenix are Monitoring. "prompt injection" mis-routed to ai — security-focused injection detection tools (Rebuff, LLM Guard) are Security. "software bill of materials" fired raw_first (bare "sbom" was mapped; spaced form wasn't; "software" is a stop word so "bill materials" bigram needed). "mobile analytics" mis-routed to frontend via "mobile"→frontend. Fixed: bigrams "workflow builder"/"workflow automation"/"visual workflow"→background; bigrams "llm monitoring"/"llm observability"→monitoring; bigram "prompt injection"→security; bigram "bill materials"→security; bigram "mobile analytics"→analytics. Also corrected stale regression test (line 880: "workflow automation n8n" now correctly routes to background, not ai). Now 1028/1028 routing tests pass.
- Probe pattern 47 (May 2026): PKM/note-taking and CDP dead zones. "obsidian", "logseq", "pkm", "zettelkasten", "zettlr", "notetaking" all fired raw_first — Logseq and Zettlr are seeded as learning-education; Obsidian is a PKM tool in the same space. Bigrams "note taking" and "second brain" also fired raw_first. "rudderstack" fired raw_first — RudderStack is an open-source Segment CDP alternative (segment already maps via "segment"→analytics). Fixed: bare "obsidian","logseq","pkm","zettelkasten","zettlr","notetaking"→learning; bigrams "note taking","second brain"→learning; bare "rudderstack"→analytics. NOTE: bare "knowledge" was NOT added — "knowledge base LLM/chatbot" queries must route to AI for RAG use cases; hyphenated "knowledge-base" and compound "knowledgebase" are already mapped→documentation at line 6433. Now 1058/1058 routing tests pass.
- Probe pattern 79 (May 2026): MLops drift / graph-RAG / DevEx / enrichment / theming dead zones. "data drift"/"concept drift"/"drift detection" fired raw_first — data drift detection tools (Evidently, NannyML, Alibi Detect, WhyLabs) are AI/MLops; bare "drift"→ai + bigrams "data drift"/"concept drift"→ai added. "graph rag" mis-routed to Database via bare "graph"→database — Graph RAG is an AI retrieval pattern (LlamaIndex, Neo4j GraphRAG); bigram "graph rag"→ai added (regression: "graph database"→database unchanged). "experimentation platform" fired raw_first — A/B experimentation platforms (Statsig, Optimizely, Split) use this term; bare "experimentation"→feature added. "entitlement management" mis-routed to Project via "management"→project — software entitlements are feature access control (LaunchDarkly, Unleash); bare "entitlement"→feature + bigram "entitlement management"→feature added. "golden path" fired raw_first — IDP/DevEx term for blessed developer workflow (Backstage, Port, Cortex); bigram "golden path"→developer added. "dx tooling"/"dx platform" fired raw_first — DX abbreviation (developer experience) unmapped; bare "dx"→developer added. "data enrichment"/"contact enrichment"/"company enrichment" fired raw_first — enrichment tools (Clearbit, Clay, Apollo) are CRM; bare "enrichment"→crm + bigrams added. "buyer intent" fired raw_first "buyer" — buyer intent data (Bombora, G2 Intent) is CRM; bare "buyer"→crm + bigram "buyer intent"→crm added. "user interface library" fired raw_first "user" — bare "user" unmapped; bigram "user interface"→frontend added. "theming" fired raw_first — CSS/UI theming tools map to Frontend; bare "theming"→frontend added. NOTE: "experiment tracking" bigram intentionally NOT added — "tracking" is in _FTS_STOP_WORDS so bigram can never fire; "ml experiment tracking" routes correctly via "ml"→ai; bare "mlflow"/"wandb" already map to ai individually. 29 new tests. Now 1567/1567 routing tests pass.
- Probe pattern 82 (May 2026): segmentation / user journey / ATS / HR dead zones. "customer segmentation" fired raw_first "customer" — segmentation analytics tools (Mixpanel, Amplitude, Braze) belong in Analytics; bigram "customer segmentation"→analytics + bare "segmentation"→analytics added. "user segmentation"/"audience segmentation" also fired raw_first; bare "audience"→analytics added. "user journey analytics"/"user journey map" fired raw_first "user" — user journey tools (FullStory, Amplitude, Hotjar) belong in Analytics; bigram "user journey"→analytics added. "applicant tracking system" → meaningful=["applicant","system"] after stop-word "tracking" stripped → raw_first; ATS tools (Lever, Ashby, Greenhouse, Workable) are CRM-adjacent; bare "applicant"→crm + bare "ats"→crm added. "hr software" → meaningful=["hr"] after stop-word "software" stripped → raw_first; HR tools (BambooHR, Rippling) map to CRM; bare "hr"→crm added. Probed but no fix needed: "customer journey" → design (correct for journey mapping), "user onboarding"→feedback (correct via bigram), "product tour"→frontend (correct), "data governance"→analytics (correct via bigram), "data catalog"→analytics (correct via bigram), "observability platform"→monitoring (correct), "compliance automation"→security (correct). 16 new tests. Now 1638/1638 routing tests pass.
- Probe pattern 83 (May 2026): infrastructure monitoring collision / capacity planning / market research dead zones. "infrastructure monitoring" mis-routed to devops via bare "infrastructure"→devops — infrastructure monitoring tools (Prometheus, Grafana, VictoriaMetrics, Netdata) belong in Monitoring; bigram "infrastructure monitoring"→monitoring added (overrides "infrastructure"→devops pre-pass). "capacity planning" fired raw_first "capacity" — both "capacity" and "planning" unmapped; infrastructure capacity planning tools (CloudHealth, Harness Cloud Cost, Spot.io) are DevOps; bare "capacity"→devops added. "market research"/"market intelligence"/"market data" fired raw_first "market" — market research & competitive intelligence tools (Crayon, Klue, Semrush, SimilarWeb) belong in Analytics; bare "market"→analytics added. Probed but no fix needed: "market segmentation"→analytics (now via "market"→analytics, previously via "segmentation"→analytics — both work), "workforce management"→project (correct), "access management"→authentication (correct), "incident management"→monitoring (correct), "load testing"→testing (correct), "synthetic monitoring"→monitoring (correct). 9 new tests. Now 1647/1647 routing tests pass.
- Probe pattern 84 (May 2026): competitive analysis / data engineering / data science / feature store / productivity dead zones. "competitive analysis"/"competitive intelligence"/"competitive pricing" fired raw_first "competitive" — competitive intelligence tools (Crayon, Klue, SimilarWeb, Semrush) belong in Analytics; bare "competitive"→analytics added. "data engineering" mis-routed to devops via bare "engineering"→devops — data engineering tools (Airbyte, Fivetran, dbt, Prefect) belong in Background Jobs; bigram "data engineering"→background added (overrides "engineering"→devops when preceded by "data"). "data science" fired raw_first — both "data" and "science" unmapped; data science tools (Jupyter, Pandas, scikit-learn, Polars) belong in AI & Automation; bigram "data science"→ai added. "feature store" mis-routed to Feature Flags via bare "feature"→feature — ML feature stores (Feast, Tecton, Hopsworks, Vertex AI Feature Store) belong in AI & Automation; bigram "feature store"→ai added. "developer productivity" fired raw_first "productivity" — "developer" is in _FTS_STOP_WORDS and gets stripped; developer productivity tools (Raycast, Warp, Pieces, Linear) belong in Developer Tools; bare "productivity"→developer added. "local first" fired raw_first — both tokens unmapped; local-first/offline-sync tools (ElectricSQL, PowerSync, Replicache, InstantDB) belong in Database; bigrams "local first"/"local-first"→database added. Known unfixable: "data integration" — "integration" is in _FTS_STOP_WORDS → strips to bare "data" with no safe single-token mapping (too ambiguous); users should use "airbyte"/"fivetran"/"etl" which are all correctly mapped. Probed but no fix needed: "payment gateway"→payments (correct), "cloud storage"→file (correct), "server side rendering"→frontend (correct), "edge function"→devops (correct for Cloudflare Workers/Deno Deploy), "event bus"→message (correct). 20 new tests. Now 1667/1667 routing tests pass.
- Probe pattern 85 (May 2026): memory leak / heap dump / SLA / terms / multi-region / tenancy / knowledge / team-collaboration dead zones. "memory leak" mis-routed to caching via bare "memory"→caching (wrong; Valgrind, LeakCanary, py-spy are Monitoring); bigram "memory leak"→monitoring added. "heap dump" mis-routed to analytics via bare "heap"→analytics (Heap.io collision; jmap, VisualVM are Monitoring); bigram "heap dump"→monitoring added. "service level agreement" fired raw_first "level" ("service" is a stop-word → ["level","agreement"] remain; SLA tools → Monitoring); bigram "level agreement"→monitoring added. "terms of service" fired raw_first "terms" ("of"+"service" both stripped; Termly/Iubenda/Termsfeed → Security Tools); bare "terms"→security added. "multi region"/"multi cloud" fired raw_first "multi" (both tokens unmapped; deployment tools → DevOps); bigrams "multi region"/"multi cloud"→devops + compounds "multiregion"/"multicloud"→devops added (NOT bare "multi" — would break "multi language"→localization). "multi tenancy" fired raw_first "multi" ("tenancy" unmapped; "tenant"→authentication already fires for "multi tenant"; gerund form "tenancy" now maps too); bare "tenancy"→authentication added. "knowledge sharing" fired raw_first "knowledge" (bare "knowledge" intentionally excluded per probe 47; "knowledge sharing" = team wiki tools → Documentation); bigram "knowledge sharing"→documentation added. "team collaboration" mis-routed to api via bare "collaboration"→api (realtime editing collision; Mattermost/Zulip are Developer Tools); bigram "team collaboration"→developer added. Probed but no fix needed: "data mesh"→devops (devops acceptable for service-mesh-adjacent tooling), "code generation"→ai dev (correct), "semantic versioning"→devops (correct), "dependency management"→developer (correct), "runtime error"→monitoring (correct via "error"), "memory profiler"→monitoring (bigram "memory profiler" already mapped). 20 new tests. Now 1687/1687 routing tests pass.
- Probe pattern 86 (May 2026): visual editor / page builder / site builder / countdown / loyalty dead zones. "visual editor component" mis-routed to testing via bare "visual"→testing (wrong; TipTap, ProseMirror, Quill, Lexical are Frontend Frameworks); bigram "visual editor"→frontend added (fires before "visual"→testing). "page builder"/"site builder" fired raw_first (both tokens unmapped; Webflow, Framer → Landing Pages); bigrams added. REGRESSION: adding "site builder"→landing caused "documentation site builder" to mis-route via "site builder" at pos 1 (beats "documentation"→documentation at pos 0 in pre-pass); fixed by adding "documentation site"→documentation bigram at pos 0. "countdown timer" fired raw_first "countdown" (unmapped; countdown UI components → Frontend); bare "countdown"→frontend added. "loyalty reward"/"loyalty program" fired raw_first "loyalty" (unmapped; loyalty tools → CRM); bare "loyalty"→crm added. Probed but no fix needed: "api testing"→api (correct, API testing tools live in api-tools), "environment variable"→security (correct via "environment"→security), "code review"→developer (correct via "review"→developer), "technical documentation"→documentation (correct via "documentation"), "data mesh"→devops (acceptable). 14 new tests. Now 1702/1702 routing tests pass.
- Probe pattern 87 (May 2026): organization management / invite / impersonation dead zones. "organization management" mis-routed to project via bare "management"→project (wrong; Clerk Orgs, WorkOS Orgs, Auth0 Organizations handle org/tenant management → Authentication); bigrams "organization management"/"org management"/"member management"→authentication added. "invite system" fired raw_first "invite" — invitation flow tools (magic-link invites, team invite emails) are Authentication-tier; bare "invite"→authentication added. "impersonation" fired raw_first — user impersonation for admin/support access (Clerk, Auth0, WorkOS impersonation) → Authentication; bare "impersonation"→authentication added. Probed but no fix needed: "background worker"/"job queue"/"message broker"→correct, "service mesh"→devops (correct), "machine to machine"→ai (unfixable: "to" is stop-word → ["machine","machine"] bigram redundant; users should search "m2m auth"/"client credentials" which route correctly), "service account"→raw_first (unfixable: "service" is stop-word → bare "account" maps to nothing, too ambiguous to add). 11 new tests. Now 1713/1713 routing tests pass.
- Probe pattern 88 (May 2026): cross-platform / cross-origin / cross-site / cross-browser dead zones. "cross platform" mis-routed to raw_first "cross" because "platform" is in _FTS_STOP_WORDS (stripped) — cross-platform dev tools (Tauri, Electron, Capacitor, React Native, Flutter) → Frontend Frameworks; bare "cross"→frontend added. Bigrams added to override bare "cross" for specific contexts: "cross origin"→api (CORS — cross-origin resource sharing, cross-origin requests → API Tools), "cross site"→security (XSS/CSRF — cross-site scripting, cross-site request forgery → Security Tools), "cross browser"→testing (cross-browser testing/compatibility → Testing Tools). Compound "crossplatform"→frontend also added. Probed but no fix needed: "ci cd pipeline"→devops (correct via "ci"), "cloud functions"→devops (correct via bigram), "serverless function"→devops (correct), "zero trust network"→security (correct via bigram "zero trust"), "webhook testing"→api (correct, API testing via webhook is api-tools tier), "openapi generator"→api (correct). 12 new tests. Now 1725/1725 routing tests pass.
- Probe pattern 89 (May 2026): code smell / technical debt / pre-push hook / open graph image dead zones. "code smell" fired raw_first "code" (unmapped; code smell detectors like CodeClimate, SonarQube → Testing Tools); bigram "code smell"→testing added. "technical debt" fired raw_first "technical" (the "tech debt" bigram already worked but the spaced "technical debt" form was missing); bigram "technical debt"→developer added. "pre push hook" mis-routed to notifications via bare "push"→notifications (wrong; Husky/Lefthook pre-push hooks are DevOps); bigram "pre push"→devops added (overrides bare "push"→notifications when preceded by "pre"). "open graph image" routed via bare "graph"→database ("open" is stop-word → ["graph","image"]); Satori/@vercel/og image generators → SEO Tools; bigram "graph image"→seo added. Probed but no fix needed: "static analysis"→testing (correct via bigram), "git hooks"→devops (correct via "git"), "code quality"→testing (correct via "quality"), "pre commit hook"→devops (correct via bigram). NOTE: "code review pull request" routes to devops via "pull request" bigram (PR automation is correct for that query pattern; "code review tool" → developer via "review"→developer). 13 new tests. Now 1738/1738 routing tests pass.
- Probe pattern 90 (May 2026): DDD / domain-driven design dead zones. "bounded context" mis-routed to frontend via bare "context"→frontend (wrong; bounded context is a DDD concept → Developer Tools); bigram "bounded context"→developer added. "aggregate root" fired raw_first (no mapping; DDD aggregate root pattern → Developer Tools); bigram "aggregate root"→developer added. "domain model" mis-routed to devops via bare "domain"→devops (wrong; domain model is a DDD layer concept → Developer Tools); bigram "domain model"→developer added. "domain event" (singular) mis-routed to devops via bare "domain" (wrong; domain events are DDD event-driven patterns like EventStoreDB, Axon → Message Queue); bigram "domain event"→message added (complements existing "domain events" plural bigram). "value object" mis-routed to file via bare "object" (wrong; value objects are immutable DDD primitives → Developer Tools); bigram "value object"→developer added. "ubiquitous language" fired raw_first (no mapping; ubiquitous language is unambiguous Eric Evans DDD vocabulary → Developer Tools); bare "ubiquitous"→developer added. Probed but no fix needed: "event sourcing"→message (correct via bigram), "domain driven design"→developer (correct via "domain driven" bigram), "hexagonal architecture"→developer (correct via bigram), "cqrs pattern"→message (correct via bare "cqrs"), "event streaming"→message (correct via "event"→message), "pub sub messaging"→message (correct via "pub sub" bigram), "saga pattern"→background (correct via "saga"). 19 new tests. Now 1757/1757 routing tests pass.
- Probe pattern 91 (May 2026): ML/AI concept dead zones — feature engineering / training / augmentation. "feature engineering"/"feature extraction"/"feature selection" mis-routed to Feature Flags via bare "feature"→feature (wrong; ML feature engineering/preprocessing concepts — Feast, Tecton, scikit-learn feature tools — belong in AI & Automation); bigrams "feature engineering"/"feature extraction"/"feature selection"→ai added (complement existing "feature store"→ai). "image labeling"/"image annotation" mis-routed to Media Server via bare "image"→media (wrong; ML data labeling tools — Label Studio, CVAT, Roboflow — belong in AI); bigrams "image labeling"/"image annotation"→ai added (labeling/annotation already maps to ai as bare tokens but image fires first). "training pipeline" mis-routed to Background Jobs via bare "pipeline"→background (wrong; Kubeflow Pipelines, ZenML, Metaflow, Kedro are MLOps → AI); bigram "training pipeline"→ai added. "training data" fired raw_first (both "training" and "data" unmapped; ML training data management → DVC, LabelStudio, Argilla → AI); bigram "training data"→ai added. "data augmentation"/"image augmentation" fired raw_first or routed via bare "image"→media (Albumentations, imgaug are AI tools); bare "augmentation"→ai + bigrams "data augmentation"/"image augmentation"→ai added. "data versioning" mis-routed to DevOps via bare "versioning"→devops (wrong; DVC, LakeFS, Delta Lake are MLOps/data-lineage tools → AI); bigram "data versioning"→ai added. 25 new tests. Now 1782/1782 routing tests pass.
- Probe pattern 92 (May 2026): Kubernetes probe / resilience pattern / compute / clickstream / user-flow dead zones. "bulkhead pattern" fired raw_first "bulkhead" (unmapped; bulkhead is a microservices resilience isolation pattern like circuit breaker → API Tools); bare "bulkhead"→api added. "exponential backoff"/"backoff strategy" fired raw_first (no mapping; exponential backoff is a retry resilience pattern → API Tools); bare "exponential"→api + bare "backoff"→api added. "liveness probe"/"readiness probe" fired raw_first for "liveness"/"readiness" (k8s container health-check terms; "probe" is not a stop-word but neither "liveness" nor "readiness" was mapped → DevOps & Infrastructure); bare "liveness"→devops + bare "readiness"→devops added (complement existing k8s→devops, kubernetes→devops). "compute platform"/"edge compute" fired raw_first "compute" (unmapped; compute platforms like Cloudflare Workers, Deno Deploy, AWS Lambda → DevOps; "faas" already mapped but "compute" generic form missing); bare "compute"→devops added. "clickstream data"/"clickstream analysis" fired raw_first "clickstream" (unmapped; clickstream analytics tools → Analytics & Metrics); bare "clickstream"→analytics added. "user flow diagram"/"user flow tool" fired raw_first via bigram miss (complement to existing "user journey"→analytics bigram; user-flow tools like Whimsical, Miro, FullStory belong in Analytics); bigram "user flow"→analytics added. Probed but no fix needed: "retry library"→api (correct), "circuit breaker"→api (correct), "kubernetes cluster"→devops (correct), "faas platform"→devops (correct), "cohort analysis"→analytics (correct), "user journey funnel"→analytics (correct). 21 new tests. Now 1803/1803 routing tests pass.
- Probe pattern 93 (May 2026): GoF design patterns / white-labeling / pricing dead zones. "design pattern"/"factory pattern"/"singleton pattern"/"observer pattern"/"strategy pattern"/"decorator pattern"/"adapter pattern"/"repository pattern" all mis-routed — "design pattern" was raw_first, "factory pattern" routed to Testing via "factory"→testing (FactoryBot collision), all others were raw_first for position-0 token; GoF software design patterns belong in Developer Tools. Fixed: bigram "design pattern"→developer (pre-pass), bigram "factory pattern"→developer (pre-pass, overrides "factory"→testing), bare "pattern"→developer (position-1 fallback for all other GoF queries where position-0 is unmapped). "white labeling" mis-routed to AI via "labeling"→ai (ML data labeling collision); "white label product" was raw_first; white-label SaaS reseller tools belong in Boilerplates; bigrams "white labeling"→boilerplate + "white label"→boilerplate added (pre-pass override). "freemium model" mis-routed to AI via "model"→ai (LLM model collision); "freemium" is a SaaS pricing concept → Payments; bare "freemium"→payments added. "seat based pricing"/"per user pricing"/"per seat pricing" were all raw_first (no pricing specificity tokens mapped); bigrams "seat based"→payments, "per user"→payments, "per seat"→payments, "tiered pricing"→payments added. Regressions guarded: "factory boy"→testing, "design system"→frontend, "data labeling"→ai, "image labeling"→ai, "usage based pricing"→payments, "api pattern"→api, "saga pattern"→background, "cqrs pattern"→message, "circuit breaker pattern"→api all unchanged. 30 new tests. Now 1833/1833 routing tests pass.
- Probe pattern 94 (May 2026): type-checker / schema-stitching / error-boundary / env-file dead zones. "schema stitching" mis-routed to Developer via bare "schema"→developer (wrong; GraphQL schema merging/stitching tools — graphql-tools/merge, The Guild Mesh — belong in API Tools); bigram "schema stitching"→api added. "type checker"/"type checking" mis-routed to Developer via bare "type"→developer (wrong; static type analysis tools mypy, pyright, tsc --noEmit belong in Testing Tools — same tier as linters/formatters); bigrams "type checker"→testing + "type checking"→testing added. "static type checking"/"static type analysis" mis-routed to Frontend via bare "static"→frontend (wrong; same mypy/pyright/tsc tools; "static"→frontend fires before "type"→developer); bigram "static type"→testing added. "error boundary react"/"error boundary component" mis-routed to Monitoring via bare "error"→monitoring (wrong; React error boundary pattern / react-error-boundary library belongs in Frontend Frameworks); bigram "error boundary"→frontend added. "env file loader"/"env file manager" mis-routed to Security via bare "env"→security (wrong; dotenv, python-dotenv, dotenvx, direnv are config-loading developer tools); bigram "env file"→developer added. Regressions guarded: "schema migration"→database, "schema validation"→developer, "type system"→developer, "type safety"→developer, "static site generator"→frontend, "static analysis"→testing, "error tracking"→monitoring, "error logging"→monitoring, "env secrets manager"→security, "graphql type generation"→api all unchanged. 22 new tests. Now 1855/1855 routing tests pass.
- Probe pattern 100 (May 2026): IP-security / task-scheduler / rate-limit dead zones. "ip firewall"/"ip blocking"/"ip allowlist"/"ip whitelist"/"ip ban" all mis-routed to Maps via bare "ip"→maps — IP access-control tools (IPBan, Cloudflare WAF, ipapi, ipblocker) belong in Security Tools; bigrams "ip firewall"/"ip blocking"/"ip allowlist"/"ip whitelist"/"ip ban"→security added before bare "ip"→maps. "ip intelligence" mis-routed to Maps — IP threat-intelligence APIs (MaxMind, ipinfo, AbuseIPDB enrichment) belong in Security; bigram "ip intelligence"→security added. "ip rate limit"/"ip rate limiting" mis-routed to Maps — per-IP rate limiting is an API Tools concept (Unkey, Upstash Ratelimit); bigram "ip rate"→api added (fires at position 0 before bare "ip"→maps). "rate limit middleware"/"rate limit per user" — "rate limiting" bigram existed but shorter "rate limit" spaced form was missing; bigram "rate limit"→api added. "task scheduler" → developer via bare "task"→developer (wrong; APScheduler, node-cron, Celery beat, agenda are Background Jobs not Developer Tools); bigrams "task scheduler"→background + "task scheduling"→background added (complement to existing "task queue"→background). Regressions: "ip geolocation"/"ip lookup"/"ip address api"→maps, "task runner"/"task automation"→developer, "rate limiting library"→api all unchanged. 14 new test cases + 5 regression guards; 1991/1991 routing tests pass.
- Probe pattern 99 (May 2026): biometric auth / e2ee / data-transform dead zones. "biometric auth"/"biometric verification" fired raw_first "biometric" (unmapped; biometric authentication tools — WebAuthn biometrics, fingerprint, face-ID SDKs — belong in Authentication; complement to existing passkey/fido2/webauthn entries); bare "biometric"→authentication + bare "biometrics"→authentication (plural) added. "e2ee library"/"e2ee messaging" fired raw_first "e2ee" (unmapped; end-to-end encryption abbreviation; "e2e"→testing correctly routes e2e testing but "e2ee" as 4-letter abbreviation for E2E Encryption was missing → Security Tools); bare "e2ee"→security added (regression guard: "e2e testing framework"→testing unchanged, "e2e encryption library"→security via existing bigram unchanged). "data transform tool"/"data transform pipeline" fired raw_first "data" ("data transformation"→background bigram already existed for the 14-char form, but shorter "data transform" 12-char form had no synonym; dbt/Spark data transformation → Background Jobs); bigram "data transform"→background added. Also fixed JSON-LD escaping in 5 more route files (alternatives.py ×4, calculator.py, explore.py, content.py, landing.py) — same _safe_ld() pattern established in compare.py fix (9c8235d). 14 new test cases + 5 regression guards; 1972/1972 routing tests pass.
- Probe pattern 98 (May 2026): DOM / XPath / duplex-streaming / input-sanitization / class-OOP dead zones. "dom manipulation"/"dom library"/"dom traversal" fired raw_first "dom" — DOM manipulation libraries (jQuery, cash.js, Umbrella.js) belong in Frontend Frameworks; bare "dom"→frontend added. Regression guards: "dom testing" and "dom snapshot" route to Testing via pre-pass bigrams that override bare "dom"→frontend. "xpath selector"/"xpath library" fired raw_first "xpath" — XPath querying (lxml, BeautifulSoup XPath, libxml2) belongs in Developer Tools; bare "xpath"→developer added. "bidirectional streaming grpc"/"bidirectional streaming websocket" mis-routed to Media via bare "streaming"→media — gRPC bidirectional streams and WebSocket full-duplex belong in API Tools; bigram "bidirectional streaming"→api + bare "duplex"→api added. "input sanitization" fired raw_first "input" — HTML sanitization libraries (DOMPurify, sanitize-html, bleach) belong in Security; bigram "input sanitization"→security added. "html sanitization" mis-routed to Frontend via bare "html"→frontend (wrong; HTML sanitization is a security concern); bigram "html sanitization"→security added (overrides "html"→frontend). "data sanitization"/"string sanitization" fired raw_first — noun form "sanitization" was unmapped while "sanitize"/"sanitizer" were already security; bare "sanitization"→security added. "class validator nestjs"/"class decorator typescript" fired raw_first "class" — class-validator (NestJS), reflect-metadata, and TypeScript class-based patterns belong in Developer Tools; bigrams "class validator"→developer, "class decorator"→developer, "class inheritance"→developer added. Regressions guarded: "dom testing"/"dom snapshot"→testing (pre-pass bigrams), "class component react"/"class based component"→frontend ("component"→frontend fires at later position), "sanitize html"→security (bare "sanitize" unchanged), "input validation"→developer ("validation"→developer unchanged). 30 new tests. Now 1958/1958 routing tests pass.
- Probe pattern 97 (May 2026): twelve-factor / soft-delete / cursor-pagination / source-map / IoC / interceptor dead zones. "twelve factor app"/"twelve-factor app"/"12 factor app"/"12-factor principles" all fired raw_first — neither "twelve" nor "12" had a synonym; 12-factor cloud-native methodology (Heroku guide, CNCF practices) belongs in DevOps; bigrams "twelve factor"→devops, "12 factor"→devops + hyphenated token forms "twelve-factor"→devops, "12-factor"→devops added. "soft delete django"/"soft deletion postgres"/"soft-delete laravel" fired raw_first — "soft" was unmapped; logical-deletion database pattern (TypeORM SoftDelete, Sequelize paranoid, Laravel SoftDeletes); bigrams "soft delete"→database, "soft deletion"→database + hyphenated "soft-delete"→database added. "cursor pagination relay"/"cursor based pagination" mis-routed to AI Dev via bare "cursor"→ai-dev (wrong; cursor-based/keyset pagination is an API design pattern — Relay spec, Prisma cursor, PostgREST range); bigrams "cursor pagination"→api, "cursor based"→api + hyphenated "cursor-pagination"→api added (fire before bare "cursor"→ai-dev). "source-map webpack"/"source-map support" fired raw_first — "source" is in _FTS_STOP_WORDS so "source map" (space form) CANNOT be fixed (bare "map"→maps wins); hyphenated form "source-map" survives as a single token; added "source-map"→developer (aligns with existing "sourcemap"→developer). "inversion of control spring"/"inversion of control tsyringe" fired raw_first — "inversion" was unmapped and "of" is a stop word; after stripping, meaningful=["inversion","control"]; added bigram "inversion control"→developer + bare "inversion"→developer (IoC containers: tsyringe, InversifyJS, Spring DI). "request interceptor axios"/"interceptor http" fired raw_first — HTTP middleware interceptors (Axios, Angular HttpClient) are API Tools; bigram "request interceptor"→api + bare "interceptor"→api added. Regressions guarded: "cursor ide alternative"→ai-dev, "cursor ai editor"→ai-dev (bare "cursor"→ai-dev unchanged), "ioc container typescript"→developer ("ioc" already mapped). 22 new tests. Now 1928/1928 routing tests pass.
- Probe pattern 96 (May 2026): usage-context / API-prefix / chaos collision dead zones. "cpu usage monitor"/"disk usage alert"/"memory usage chart"/"resource usage dashboard" mis-routed to Invoicing (bare "usage"→invoicing) or Caching (bare "memory"→caching) — these are system-resource monitoring queries (Datadog, Prometheus, Grafana); bigrams "cpu usage"→monitoring, "disk usage"→monitoring, "memory usage"→monitoring, "resource usage"→monitoring added (fire before bare-token fallbacks). "api security scanner"/"api security testing" mis-routed to API via bare "api"→api at position 0 (wrong; security scanners 42crunch/Wallarm → Security); bigram "api security"→security added. "api monitoring tool"/"api observability platform" mis-routed to API (wrong; API monitoring/observability Treblle/Helicone/Moesif → Monitoring); bigrams "api monitoring"→monitoring + "api observability"→monitoring added. "api contract testing"/"api contract pact" mis-routed to API (wrong; contract testing Pact/Dredd → Testing); bigram "api contract"→testing added. "fault injection testing"/"fault injection chaos" mis-routed to Developer via bare "injection"→developer (DI collision; fault injection is chaos/resilience testing Gremlin/Chaos Toolkit → Testing); bigram "fault injection"→testing added. "chaos"→devops reclassified to "chaos"→testing — Gremlin, LitmusChaos, ChaosMesh, Chaos Monkey are testing/fault-injection tools not DevOps infra. Regressions guarded: "usage based pricing"→payments, "usage billing saas"→payments (billing bigrams unchanged), "usage tracking"→invoicing (bare "usage" still fires without cpu/disk/memory/resource prefix), "dependency injection"→developer + "constructor injection"→developer (bare "injection"→developer unchanged), "memory cache redis"→caching (bare "memory"→caching fires when no "usage" suffix), "contract testing pact"→testing + "api gateway kong"→api unchanged. 31 new tests. Now 1906/1906 routing tests pass.
- Probe pattern 95 (May 2026): component-testing / AI-router / procedure-call / embedding dead zones. "component testing react"/"component testing storybook"/"component testing library" mis-routed to Frontend via bare "component"→frontend (wrong; React Testing Library, Storybook interaction tests, Testing Library are Testing Tools); bigram "component testing"→testing added (overrides bare "component"). "ai router litellm"/"ai router openrouter" mis-routed to Frontend via bare "router"→frontend (wrong; AI model routers like LiteLLM, OpenRouter, PortKey route requests between LLMs → AI & Automation); bigram "ai router"→ai added. "remote procedure call"/"procedure call rpc" mis-routed to Monitoring via bare "call"→monitoring (on-call collision; RPC tools like tRPC, gRPC, Connect-RPC → API Tools); bigram "procedure call"→api added. "widget embedding html"/"iframe embedding react" mis-routed to AI via bare "embedding"→ai (wrong; HTML-embed widgets/iframes — not vector embeddings — belong in Developer Tools); bigrams "widget embedding"→developer + "iframe embedding"→developer added. Regressions guarded: "component library react"→frontend, "component design system"→frontend, "react router v7"→frontend, "vue router navigation"→frontend, "on call scheduling"→monitoring, "word embedding model"→ai, "text embedding model"→ai all unchanged. 20 new tests. Now 1875/1875 routing tests pass.
- Probe pattern 81 (May 2026): RevOps / ABM / CDP / Customer-Success dead zones. "revops" fired raw_first — Revenue Operations platforms (Clari, Gong) are CRM; bare "revops"→crm added. "demand generation"/"demand gen" fired raw_first "demand" — demand-gen/ABM tools (6sense, Demandbase, Terminus) are CRM; bare "demand"→crm added. "abm tool"/"abm software" fired raw_first — Account-Based Marketing abbreviation unmapped; bare "abm"→crm added. "account based marketing"/"account based selling" fired raw_first "account" — ABM queries; bigram "account based"→crm added. "customer success tool" fired raw_first "customer" — CS platforms (Gainsight, ChurnZero, Vitally) are CRM; bigram "customer success"→crm added. "cdp tool"/"cdp alternative" fired raw_first — Customer Data Platform abbreviation (Segment, RudderStack, mParticle); bare "cdp"→analytics added. "customer data platform" → meaningful=["customer","data"] ("platform" stripped) → raw_first; bigram "customer data"→analytics added. "net promoter score" fired raw_first "net" — NPS surveys; "nps" bare was already mapped→feedback but multi-word form needed bigram; "net promoter"→feedback bigram added. Regressions guarded: "revenue analytics"→analytics (bare "revenue"), "lead scoring crm"→crm (bare "lead"), "sales intelligence"→crm (bare "sales"), "nps survey"→feedback (bare "nps"), "data pipeline etl"→background ("pipeline"→background unchanged) all intact. 22 new tests. Now 1622/1622 routing tests pass.
- Probe pattern 80 (May 2026): attribution / UTM / PLG / CRO / DAU/MAU dead zones. "attribution tool"/"click attribution"/"marketing attribution"/"multi touch attribution" fired raw_first — attribution analytics tools (Rockerbox, Triple Whale, Northbeam) belong in Analytics; bare "attribution"→analytics, bigrams "click attribution"→analytics (beats "click"→cli collision), "marketing attribution"→analytics, "multi touch"→analytics added. "utm tracker" fired raw_first — "tracking" in _FTS_STOP_WORDS strips to bare "utm"; UTM parameter builders belong in Analytics; bare "utm"→analytics added. "cro tool" fired raw_first — CRO (Conversion Rate Optimization) tools belong in Analytics; bare "cro"→analytics added. "conversion tracking" fired raw_first — "tracking" stripped → bare "conversion" unmapped; bare "conversion"→analytics added (regression guard: "conversion rate" bigram fires first for "conversion rate optimization"). "product led growth" fired raw_first "product" — PLG; bigram "led growth"→analytics fires at i=1 in pre-pass; bare "plg"→analytics added. "growth hacking tool" fired raw_first — bigram "growth hacking"→analytics added. "user activation"/"activation rate" fired raw_first — bare "activation"→analytics added. "dau tracking"/"mau dashboard" fired raw_first — bare "dau"/"mau"→analytics added. "heap analytics" fired raw_first "heap" — Heap.io → Analytics; bare "heap"→analytics added (no "memory heap" collision; "memory"→caching fires first). "inspectlet alternative" fired raw_first — Inspectlet session recording → Analytics. Confirmed already-working: "session replay"→analytics (bigram), "heatmap"→analytics, "funnel"→analytics, "retention"→analytics, "churn"→analytics, "product analytics"→analytics, "revenue attribution"→analytics, "type safety"→developer, "knowledge graph database"→database. Known limitation: "self hosted analytics/cms/auth" routes to devops via "self hosted" bigram (pre-pass position-0 bigram fires before qualifier token; no tractable fix without breaking "self hosted alternative" patterns — accept). 33 new tests. Now 1600/1600 routing tests pass.
- Probe pattern 78 (May 2026): business intelligence / headless automation / kill switch / user behavior / multivariate dead zones. "business intelligence" fired raw_first "business" — bare "bi"→analytics existed but the spaced bigram was missing; BI tools (Metabase, Redash, Superset) live in Analytics; bigram "business intelligence"→analytics added. "headless automation" mis-routed to CMS via bare "headless"→cms — headless browser automation tools (Puppeteer, Playwright-cluster, Browserless) belong in Testing; bigram "headless automation"→testing added (pre-pass bigram fires before bare "headless"→cms). "kill switch" fired raw_first "kill" — no synonym existed; kill switch is a canonical feature-flag pattern; bigram "kill switch"→feature added. "user behavior" fired raw_first "user" — bare "user" has no synonym; user behaviour analytics tools (PostHog, FullStory, Heap) belong in Analytics; bigram "user behavior"→analytics added ("user behavior tracking" → meaningful=["user","behavior"] after stop-word strip → bigram fires). "multivariate" fired raw_first — no bare token mapping; A/B/multivariate testing is core feature-flag use case; bare "multivariate"→feature added. Regressions guarded: "business logic validation"→developer, "headless cms"→cms, "headless browser testing"→testing, "feature flag toggle"→feature, "ab testing tool"→feature, "user research tool"→feedback, "user authentication"→authentication all unchanged. 17 new tests. Now 1538/1538 routing tests pass.
- Probe pattern 77 (May 2026): "image to text" / "pdf to text" stop-word-stripped bigrams. "to" is in _FTS_STOP_WORDS — "image to text" strips to bigram "image text"; "pdf to text" strips to bigram "pdf text". Without mappings, "image to text api" mis-routed to Media via bare "image"→media; "pdf to text python" mis-routed to File via bare "pdf"→file. OCR / document-extraction tools (EasyOCR, Tesseract, AWS Textract, LlamaParse) are AI & Automation. Added "image text"→ai and "pdf text"→ai bigrams. Regressions guarded: "image upload react"→frontend, "image captioning api"→ai, "pdf viewer react"→frontend, "pdf parsing python"→ai all unchanged. 6 new tests. Now 1521/1521 routing tests pass.
- Probe pattern 76 (May 2026): AI document-processing + image-AI dead zones. "image captioning" mis-routed to Media via bare "image"→media — AI image captioning models (BLIP, LLaVA, GPT-4V) belong in AI & Automation; bigram "image captioning"→ai added. "text extraction" fired raw_first — bare "text" has no synonym; NLP text extraction tools (spaCy, Amazon Textract, Tika) belong in AI; bigram "text extraction"→ai added. "document parsing" mis-routed to Database via bare "document"→database — AI document parsers (LlamaParse, unstructured.io, docling) belong in AI & Automation; bigram "document parsing"→ai added. "pdf parsing" mis-routed to File Management via bare "pdf"→file — PDF parsing for AI/RAG (LlamaParse, pdfplumber, PyPDF2) belongs in AI; bigram "pdf parsing"→ai added. "document understanding" mis-routed to Database via "document"→database — AI document understanding models (LayoutLM, DocTR, Azure Document Intelligence) belong in AI; bigram "document understanding"→ai added. Regressions guarded: "document database"→database, "document chunker python"→database, "document qa tool"→ai (existing bigram), "image upload react"→frontend, "pdf viewer react"→frontend all unchanged. 15 new tests. Now 1515/1515 routing tests pass.
- Probe pattern 75 (May 2026): LLM response streaming / HTTP streaming / Node.js stream dead zones. "streaming api response nodejs" mis-routed to Media via bare "streaming"→media — HTTP/LLM response streaming belongs in API Tools; bigram "api response"→api fires before bare "streaming" in the second loop (position 1 checks "api response" before "streaming" at position 0 fires). "stream ai response" mis-routed to Message Queue via bare "stream"→message — AI streaming output belongs in AI & Automation; bigram "stream ai"→ai added. "token streaming react" mis-routed to Authentication via bare "token"→auth — LLM token streaming belongs in AI & Automation; bigram "token streaming"→ai added. "readable stream nodejs" mis-routed to Message Queue via bare "stream"→message — Node.js Readable Streams are API Tools; bigram "readable stream"→api added. "event source javascript" UNFIXABLE via bigram — "source" is in _FTS_STOP_WORDS, stripped to "event"+"javascript", bare "event"→message fires; added bare "eventsource"→api for compound form only, documented as fundamental gap. Also added "streaming response"→api bigram and "llm streaming"→ai (reverse of "streaming llm" already mapped). Regressions guarded: "video streaming api"→media (fires via "video"→media before "streaming api" — no "streaming api" bigram added), "live streaming"→media, "audio streaming"→media, "data streaming kafka"→message all unchanged. 19 new tests. Now 1500/1500 routing tests pass.
- Probe pattern 74 (May 2026): VoIP/telephony voice collision / service-worker PWA / product-tour dead zones. "voice call api"/"voice calling sdk" mis-routed to AI via bare "voice"→ai — these are VoIP/telephony queries (Twilio Voice, Telnyx); bigrams "voice call"→notifications + "voice calling"→notifications added. "phone call api" mis-routed to Authentication via bare "phone"→auth (phone auth collision) — phone call APIs are telephony → Notifications; bigram "phone call"→notifications added. "service worker pwa" mis-routed to Background Jobs via bare "worker"→background — "service" is in _FTS_STOP_WORDS so "service worker" bigram can NEVER form; instead added "worker pwa"→frontend bigram (covers "service worker pwa" after stop-word stripping). "feature tour component" mis-routed to Feature Flags via bare "feature"→feature — product tour components belong in Frontend Frameworks; bigram "feature tour"→frontend added (fires before bare "feature"→feature). "walkthrough guide library"/"introjs alternative"/"interactive walkthrough tool"/"interactive demo library" all fired raw_first — product tour/onboarding UI libs belong in Frontend; bare "walkthrough"→frontend, bare "introjs"→frontend, bigram "interactive walkthrough"→frontend, bigram "interactive demo"→frontend added. NOTE: bare "interactive"→frontend was considered but dropped — breaks "interactive tutorial"→learning regression. 23 new tests. Now 1481/1481 routing tests pass.
- Probe pattern 73 (May 2026): "click" UI collision / file-upload component / WebGL graphics / masked-input dead zones. "glsl shader" fired raw_first — GLSL is the WebGL shader language; Three.js/Babylon.js context → Frontend Frameworks; bare "glsl"→frontend added. "shader programming"/"opengl library" fired raw_first — WebGL-adjacent graphics tokens; bare "shader"→frontend and "opengl"→frontend added. "file upload react" mis-routed to File Management via "upload"→file — file upload UI components (react-dropzone, FilePond, Uppy) belong in Frontend Frameworks; bigram "file upload"→frontend added (overrides bare "upload"→file at position 0). "image upload widget" mis-routed to Media Server via "image"→media — image upload UI components belong in Frontend; bigram "image upload"→frontend added. "image editor javascript" mis-routed to Media via "image"→media — embedded image editor components (Filerobot, react-image-editor) are Frontend; bigram "image editor"→frontend added. "right click menu" mis-routed to CLI via "click"→cli (Python Click) — context menu UI libs belong in Frontend; bigram "right click"→frontend added. "click outside hook" mis-routed to CLI — useClickOutside hooks are Frontend; bigram "click outside"→frontend added. "click heatmap" mis-routed to CLI via "click"→cli — heatmap analytics tools are Analytics; bigram "click heatmap"→analytics added. NOTE: "click tracking" bigram CANNOT fire — "tracking" is in _FTS_STOP_WORDS. "masked input react" fired raw_first — imask.js, Cleave.js, react-imask belong in Frontend; bare "masked"→frontend + bigram "masked input"→frontend added. "number input component" mis-routed to Developer via "number"→developer — number input UI widgets (react-number-format) belong in Frontend; bigrams "number input"→frontend and "number format"→frontend added. "photo crop react" fired raw_first "photo" — Cropper.js, react-image-crop belong in Frontend; bigram "photo crop"→frontend added. Also removed 3 duplicate _CAT_SYNONYMS entries (transifex×2, phrase×2, meta tags×2 — same value both times, harmless but messy). 23 new tests. Now 1458/1458 routing tests pass.
- Probe pattern 72 (May 2026): Date/time picker UI + SEO structured data dead zones. "time picker" fired raw_first — both "time" and "picker" individually unmapped; time picker UI components (react-time-picker, flatpickr, react-datepicker time mode) belong in Frontend Frameworks; bigram "time picker"→frontend and bare "timepicker"→frontend added. "structured data" fired raw_first "structured" — "structured" was intentionally removed (probe 39 context: "structured output" LLM queries must route to AI); the missing bare token means "structured data" (JSON-LD/schema.org) has no category; bigram "structured data"→seo added. "schema markup" routed via bare "schema"→developer (schema validation collision) — schema.org markup generators belong in SEO Tools; bigram "schema markup"→seo added (fires before bare "schema"→developer). "schema org" also routed via bare "schema"→developer; bigram "schema org"→seo added. Regressions guarded: "schema validation library"→developer, "json schema validator"→developer (bare "schema" unchanged), "date picker react"→frontend (bare "date" unchanged), "structured logs"→logging (bare "logs"→logging fires before bigram scan), "structured output llm"→ai ("output"→ai fires). 14 new tests. Now 1435/1435 routing tests pass.
- Probe pattern 71 (May 2026): AI prompting techniques + auth/security acronym dead zones. "constitutional ai" fired raw_first "constitutional" — Constitutional AI (Anthropic's alignment method) belongs in AI Standards; bigram "constitutional ai"→ai standards + bare "constitutional"→ai standards added. "chain of thought" fired raw_first "chain" — after stop-word removal ("of" stripped), becomes "chain thought"; bigram "chain thought"→ai added (covers "chain of thought prompting/reasoning"). "few shot" and "zero shot" fired raw_first — few-shot and zero-shot prompting/learning belong in AI & Automation; bigrams "few shot"→ai and "zero shot"→ai added. "rls" fired raw_first — Row Level Security (PostgreSQL/Supabase RLS) is access control → Authentication (same tier as rbac/permissions); bare "rls"→authentication and bigram "row level"→authentication added. Regressions guarded: "rlhf training"→ai, "rbac permissions"→authentication, "zero downtime deploy"→devops all unchanged. 15 new tests. Now 1421/1421 routing tests pass.
- Probe pattern 70 (May 2026): Frontend UI / color / font / payments micro dead zones. "palette" fired raw_first — color palette tools (Coolors, Paletton, ColorHunt) belong in Frontend Frameworks; bare "palette"→frontend added. "typeface" fired raw_first — font/typeface tools (Fontjoy, Bunny Fonts, fontpair) belong in Frontend Frameworks; bare "typeface"→frontend added. "micropayment"/"micropayments" fired raw_first — micropayment APIs belong in Payments; both forms added. "virtualization"/"virtualisation" fired raw_first — virtual list/window libraries (react-virtual, TanStack Virtual, react-window) belong in Frontend Frameworks; both spellings added. "multi select", "progress bar", "skeleton loader", "loading spinner" fired raw_first — all common UI widget component queries belong in Frontend Frameworks; bigrams added. Regressions guarded: "color picker"→frontend, "stripe payments"→payments, "payment processing"→payments all unchanged. 20 new tests. Now 1406/1406 routing tests pass.
- Probe pattern 69 (May 2026): DB GUI / distributed SQL / VoIP / big-data dead zones. "pgadmin" fired raw_first — pgAdmin is a PostgreSQL GUI admin tool and belongs in Developer Tools (same tier as TablePlus/DBeaver, which were already mapped); bare "pgadmin"→developer added. "trino"/"presto" fired raw_first — both are distributed SQL query engines (same tier as ClickHouse/DuckDB) and belong in Database; bare mappings added. "voip"/"sip" fired raw_first — VoIP/SIP tools (Twilio Voice, Telnyx, Vonage) belong in Notifications; bare "voip"→notifications + "sip"→notifications added. "hbase"/"druid"/"rethinkdb"/"janusgraph" fired raw_first — NoSQL/OLAP/graph DBs → Database added. "hadoop" fired raw_first — Apache Hadoop is a MapReduce batch processor → Background Jobs (same tier as Spark, ETL pipelines). Regressions guarded: "kafka"→message, "tableplus"→developer, "dbeaver"→developer all unchanged. 22 new tests. Now 1386/1386 routing tests pass.
- Probe pattern 68 (May 2026): MCP protocol spaced form / file-watcher / AI alignment dead zones. "model context protocol" misfired to AI & Automation via bare "model"→ai — MCP is the Model Context Protocol and belongs in MCP Servers; bigram "model context"→mcp added. "context protocol" misfired to Frontend via bare "context"→frontend — in AI-agent context, "context protocol" means MCP; bigram "context protocol"→mcp added (regression: "react context api"/"context menu react" still fire via react/context→frontend correctly). "ai alignment" fired raw_first — neither "ai" nor "alignment" is in _CAT_SYNONYMS; AI alignment/safety tools belong in AI Standards; bigram "ai alignment"→"ai standards" added. "file watcher" fired raw_first — neither "file" nor "watcher" mapped; filesystem watchers (Chokidar, Watchman, nodemon) are Developer Tools; bare "watcher"→developer + bigram "file watcher"→developer added. Regressions guarded: "model deployment"→ai, "react context api"→frontend, "context menu react"→frontend all unchanged. 11 new tests. Now 1364/1364 routing tests pass.
- Probe pattern 67 (May 2026): ETL / infrastructure-metrics / metered-billing / back-office dead zones. "extract transform load" misfired to Testing via bare "load"→testing (load-testing collision) — ETL pipelines (Airbyte, dbt, Prefect, Fivetran) belong in Background Jobs; bigrams "transform load"/"extract transform"→background added. "metrics collection" misfired to Analytics via bare "metrics"→analytics — infrastructure metrics tools (Prometheus, VictoriaMetrics, StatsD, Telegraf) belong in Monitoring; bigram "metrics collection"→monitoring added (bare "metrics" still fires analytics for product-metrics queries). "metered billing" / "usage billing" / "usage based billing" misfired to Invoicing via bare "metered"/"usage"→invoicing — usage-based billing tools (Metronome, Orb, Lago, Stripe Billing Meters) belong in Payments; bigrams "metered billing"/"usage billing"/"usage based"/"consumption billing"→payments added (bare "usage" still fires invoicing). "backoffice builder" fired raw_first — back-office admin builders (Retool, Appsmith, Tooljet, Budibase) belong in Developer Tools; bare "backoffice" and bigram "back office"→developer added. Also updated 2 stale tests (lines 760-761) that expected "invoicing" for "usage based billing"/"metered billing api" — now correctly expect "payments". 14 new tests. Now 1353/1353 routing tests pass.
- Probe pattern 66 (May 2026): UI-component second-pass + mind-map/collaborative dead zones. "mind map tool" misfired to Maps & Location via bare "map"→maps — mind-map tools (Markmap, React Flow, mxGraph) belong in Developer Tools. "mind mapping react" and "mindmap react" fired raw_first — both "mind" and "mapping"/"mindmap" were unmapped. "markdown editor react" misfired to Documentation via "markdown"→docs — markdown editor components (TipTap, Milkdown, ProseMirror, Plate) are Frontend Frameworks. "calendar component react" misfired to Scheduling via "calendar"→scheduling — calendar UI libs (React Big Calendar, FullCalendar) are Frontend Frameworks. "toast notification react" misfired to Notifications via "toast"→notifications — toast UI libs (react-hot-toast, Sonner, react-toastify) are Frontend Frameworks. "breadcrumb navigation" fired raw_first — "breadcrumb" unmapped. "collaborative coding tool" misfired to API Tools via "collaborative"→api — pair-programming IDE tools (CodeTogether, Tuple) are Developer Tools. Fixed: bigrams "mind map"/"mind mapping"→developer, bare "mindmap"→developer; bigram "markdown editor"→frontend; bigram "calendar component"→frontend; bigram "toast notification"→frontend; bare "breadcrumb"→frontend; bigram "collaborative coding"→developer. Regressions guarded: "map tiles"→maps, "markdown parser"→docs, "calendar api"→scheduling, "toast pop up"→notifications, "collaborative editing"→api all unchanged. 21 new tests. Now 1339/1339 routing tests pass.
- Probe pattern 65 (May 2026): realtime-collaboration dead zones + multi-model DB collision. "operational transform sharedb" fired raw_first "operational" — OT algorithm for realtime collab (ShareDB, Firepad) belongs in API Tools. "shared editing yjs" fired raw_first "shared" — Yjs, Automerge shared-editing context is API Tools. "presence awareness liveblocks" fired raw_first "presence" — Liveblocks/PartyKit presence APIs belong in API Tools. "live cursors liveblocks" fired raw_first "live" — cursor presence is API Tools. "multi model database surreal" misfired to AI via "model"→ai before "database"→database fired — SurrealDB, FaunaDB, ArangoDB multi-model DBs belong in Database. STOP-WORD TRAP: "presence tracking" bigram CANNOT fire since "tracking" is in _FTS_STOP_WORDS; stripped queries ["presence","X"] rely on X token routing instead. Fixed: bigrams "operational transform"/"shared editing"/"presence awareness"/"live cursors"→api; bigram "multi model"→database. 15 new tests. Now 1318/1318 routing tests pass.
- Probe pattern 63 (May 2026): UI component dead zones / modal collision. "modal component react"/"modal window library" misfired to AI & Automation via bare "modal"→ai (Modal.com serverless Python collision) — React/Vue/shadcn modal dialog components live in Frontend Frameworks. "dropdown component"/"dropdown menu react" fired raw_first — "dropdown" unmapped; dropdown UI components (Radix Dropdown, Headless UI) live in Frontend. "sorting library javascript" fired raw_first — "sorting" unmapped; table/list sorting (TanStack Table, sortablejs) lives in Frontend. "infinite scroll react" fired raw_first — both tokens unmapped (dual dead zone pattern 23); infinite scroll (react-infinite-scroll, TanStack Virtual) lives in Frontend. "select component accessible" fired raw_first — bare "select" skipped (SQL SELECT ambiguity), bigram "select component" added instead. Fixed: bigrams "modal component"/"modal window"→frontend (override "modal"→ai), bare "dropdown"→frontend + bigram "dropdown menu"→frontend, bare "sorting"→frontend, bigram "infinite scroll"→frontend, bigram "select component"→frontend. Regressions guarded: bare "modal"→ai (Modal.com), "modal dialog component"→frontend (probe 38 bigram) unchanged. 15 new tests. Now 1292/1292 routing tests pass.
- Probe pattern 64 (May 2026): category-semantic mismatch — tool class routed to wrong high-level bucket. Three patterns identified: (a) "storybook" was mapped→testing (component test runner connotation) but Storybook is primarily a component development environment — changed to frontend; "storybook alternative"/"storybook component" now correctly route to Frontend Frameworks. (b) Bare noun owned by wrong bucket causes component query misfire: "image"→media so "image cropper" misfired to Media Server (Cropper.js, react-image-crop are frontend UI components) — bigram "image cropper"→frontend added before bare "image". Similarly "pdf"→file so "pdf viewer react" misfired to File Management (react-pdf, pdfjs-dist are frontend embed components) — bigram "pdf viewer"→frontend added. (c) raw_first dead zone where both tokens are unmapped: "custom element" fired raw_first "custom" (no synonym) — "custom-element" (hyphenated) was mapped but space-separated form was missing; bigram "custom element"→frontend added. Strategy: when routing test shows raw_first for a compound, check if the hyphenated or camelCase form is already mapped — add the space-separated bigram to close the gap. 11 new tests. Now 1303/1303 routing tests pass.
- Probe pattern 62 (May 2026): git branching strategy / preview-environment dead zones. "trunk based development" misfired to frontend via "trunk"→frontend (Trunk.io build tool collision) — trunk-based development is a git branching strategy (DevOps). "gitflow branching" fired raw_first "gitflow" — GitFlow is a git branching model (DevOps). "branch protection rules" fired raw_first "branch" — GitHub branch protection rules tooling lives in DevOps. "feature branch deployment" misfired to feature-flags via "feature"→feature — git feature branches are DevOps workflows. "preview environment deployment" / "staging environment" / "ephemeral environment" all misfired via "environment"→security — deploy preview/staging/ephemeral environments are DevOps infra (Vercel previews, Railway, ephemeral CI envs). Fixed: bigram "trunk based"→devops (overrides "trunk"→frontend), bare "gitflow"→devops, bigram "branch protection"→devops, bigram "feature branch"→devops (distinct from "feature flag" bigram), bigrams "preview environment"/"staging environment"/"ephemeral environment"→devops. Regressions guarded: "trunk linter"/"trunk io"→frontend, "feature flag toggle"/"feature toggle"→feature, "environment variables secrets"→security, "development environment"→devops all unchanged. 19 new tests. Now 1277/1277 routing tests pass.
- Probe pattern 61 (May 2026): mono-repo spaced form / PR-automation / security-testing stop-word collision / dev-server / commit-lint dead zones. "mono repo tool" fired raw_first — "monorepo" was mapped but the spaced form "mono repo" wasn't (pattern 55: short-form gaps). "pr automation github" fired "automation"→ai — "pr" bare token unmapped, PR bots (Danger.js, Renovate, Dependabot) live in DevOps. "static application security testing" fired "static"→frontend — "application" is a stop word leaving adjacent tokens ["static","security","testing"]; bigram "security testing"→security added at pos 1-2 overrides "static" at pos 0 (pattern 54: stop-word stripping reveals hidden first-token collision). "dev server vite" fired raw_first — "dev" and "server" both unmapped; Vite/webpack-dev-server are Frontend Frameworks. "commit lint setup" fired "lint"→testing — "commitlint" bare token was devops but spaced "commit lint" hit "lint"→testing first; bigram added. Fixed: "mono repo"→developer, "pr automation"/"pr bot"→devops, "security testing"→security, "dev server"→frontend, "commit lint"→devops. 15 new tests. Now 1258/1258 routing tests pass.
- Probe pattern 60 (May 2026): model-card dead zone. "model card generator" mis-routed to AI & Automation via "model"→ai — model cards are structured documentation for ML models (AI safety/standards, Hugging Face Cards); they belong in AI Standards. Fixed: bigrams "model card"/"model cards"→ai standards. Regressions guarded: "language model api"→ai, "model evaluation benchmark"→ai standards unchanged. 2 new tests. Now 1243/1243 routing tests pass.
- Probe pattern 59 (May 2026): feedback-form collision / 3D dead zone / boilerplate-codegen collision / document-storage mis-route. "feedback form builder" routed to Feedback & Reviews via "feedback"→feedback — form builders for collecting feedback (Typeform, Tally) belong in Forms & Surveys. "3d modeling tool" fired raw_first — "3d" had no mapping; Three.js, Blender API, Babylon.js are Creative Tools. "boilerplate code generator" routed to AI Dev Tools via "code generator" bigram at pos 1 — bigram "boilerplate code"→boilerplate added at pos 0 to fire first. "document storage api" routed to Database via "document"→database — document storage APIs (Cloudflare R2, Filestack) are File Management. Fixed: bigrams "feedback form"→forms, "3d modeling"→creative; bare "3d"→creative; bigram "boilerplate code"→boilerplate; bigram "document storage"→file. 13 new tests. Now 1240/1240 routing tests pass.
- Probe pattern 58 (May 2026): learning / api-docs / crm / landing-pages / newsletters / image-optimization dead zones. "coding bootcamp" misfired to ai-dev via "coding"→ai dev — bootcamp platforms are Learning. "developer tutorials" had "tutorials" unmapped → raw_first. "api docs generator" misfired to api-tools via "api"→api — API documentation tools (Swagger UI, Stoplight, ReadMe) are Documentation. "customer relationship management" misfired to project via "management"→project — CRM tools belong in CRM. "sales page creator" misfired to crm via "sales"→crm — sales landing page builders belong in Landing Pages. "newsletter monetization" misfired to email-marketing via "newsletter"→email — newsletter business tools (Beehiiv, Substack) belong in Newsletters & Content. "image optimization cdn" misfired to media via "image"→media — image CDN/optimization tools (Cloudinary, imgix) are File Management. NOTE: "newsletter platform" bigram is dead code — "platform" is in _FTS_STOP_WORDS. Fixed: bigrams "coding bootcamp"/"api docs"/"api documentation"/"customer relationship"/"sales page"/"newsletter monetization"/"image optimization"; bare tokens "tutorials"/"tutorial"/"bootcamp". 20 new tests. Now 1227/1227 routing tests pass.
- Probe pattern 57 (May 2026): changelog-widget collision / document-signing dead zone / pre-commit hooks collision. "changelog widget" mis-routed to devops via "changelog"→devops (git changelog generators) — product changelog widgets (AnnounceKit, Beamer, Featurebase) live in Feedback & Reviews. "document signing" mis-routed to database via "document"→database (MongoDB document-store terminology) — e-signature APIs (DocuSign, HelloSign, PandaDoc) live in Forms & Surveys. "pre commit hooks" mis-routed to frontend via "hooks"→frontend (React hooks) — pre-commit hook runners (Husky, pre-commit, Lefthook) live in DevOps. "esign api" fired api via bare "api" — e-signature tools should route to Forms. Fixed: bigrams "changelog widget"/"changelog embed"→feedback; bigrams "document signing"→forms; bare "esign"→forms; bigram "pre commit"→devops (matches existing "pre-commit"→devops hyphenated form). NOTE: "whats new widget" is unfixable — "new" is in _FTS_STOP_WORDS so bigram "whats new" can never fire; documented as known gap. 14 new tests. Now 1207/1207 routing tests pass.
- Probe pattern 56 (May 2026): AI-prefix wrong-subcategory: memory / chat collisions. "ai memory" → "memory"→caching misfired; AI agent memory (MemGPT, Mem0, Zep) live in AI & Automation. "ai chat" → "chat"→customer misfired; AI chatbot builders (Chatbase, OpenChat) live in AI & Automation. Fixed: bigrams "ai memory"→ai, "ai chat"→ai. Regressions guarded: "redis memory cache"→caching, "live chat widget"→customer unchanged. Now 1193/1193 routing tests pass.
- Probe pattern 55 (May 2026): AI routing dead zones: red-team / context-management / AI observability / AI deployment. "red team" spaced form fired raw_first — only "redteam", "red-team", "red teaming" were mapped. "context management" fired "context"→frontend (wrong; LLM context management tools are AI & Automation, not React Context API). "ai tracing"/"ai observability" fired "tracing"/"observability"→monitoring (wrong; LLM trace viewers like LangSmith are AI Dev Tools). "ai deployment" fired "deployment"→devops (wrong; AI model serving tools like BentoML, Modal are AI & Automation). Fixed: bigram "red team"→ai standards; bigram "context management"→ai; bigrams "ai tracing"/"ai observability"→ai dev; bigram "ai deployment"→ai. Now 1186/1186 routing tests pass.
- Probe pattern 54 (May 2026): AI Dev Tools routing precision / GitHub Copilot collision / file-hosting dead zone. cursor/windsurf/copilot were mapped to bare "ai" (routes to AI & Automation too) — changed to "ai dev" to precisely target AI Dev Tools. "github copilot alternative" fired "github"→devops before "copilot"→ai dev — bigram "github copilot"→ai dev added. "ai ide" fired "ide"→developer — bigram "ai ide"→ai dev added. "file hosting" fired "hosting"→devops — bigram "file hosting"→file added. "automation workflow" (reversed) fired "automation"→ai — bigram "automation workflow"→background added (symmetric with "workflow automation"). Now 1168/1168 routing tests pass.
- Probe pattern 54b (autonomous loop, May 2026): RASP / codemod / AST transformation dead zones. "rasp" fired raw_first — the four ASTT acronyms (sast/dast/iast/rasp) were missing rasp; Contrast Security, Sqreen/Datadog App Protection, AppSentinel unreachable. "runtime protection" bigram also added. "codemod"/"codemods" mis-routed to frontend/database via surrounding tokens ("typescript"→frontend at pos 1, "migration"→database at pos 1); bare tokens fire first after fix → developer. "jscodeshift" fired raw_first — the most popular JS AST transform library (Facebook, 21k★) was completely missing. "ast" bare token fired raw_first — AST Explorer, ts-morph, acorn unreachable; treesitter/tree-sitter already existed but bare "ast" was missing. "acorn" (foundational Acorn.js AST parser, 27k★) also added. Strategy: audit ALL siblings in a named acronym family (ASTT: sast/dast/iast/rasp) to find the missing member. 23 new tests added.
- Probe pattern 53 (May 2026): codegen-bigram collision / realtime-database mis-route / smart-contract dead zones. "openapi code generator", "swagger code generator", "graphql code generator", "protobuf code generation" all mis-routed to ai-dev via "code generator"/"code generation" bigram firing at position 1+ — overriding the correct first-token (openapi/swagger/graphql/protobuf). Strategy (Pattern 44 in test file): for any bigram X→catA, probe "[domain-tool] [first-word-of-X]" where domain-tool belongs to catB; add pos-0 bigram if result is wrong. Fixed: "openapi code"→api, "swagger code"→documentation, "graphql code"→api, "protobuf code"→developer, "proto code"→developer. "realtime database firebase"/"realtime database sync" mis-routed to api via bare "realtime"→api — Firebase RT DB / Supabase Realtime belong in Database; bigram "realtime database"→database added. "smart contract solidity"/"smart contracts ethereum" mis-routed to testing via bare "contract"→testing (Pact contract testing); "smart contract"→developer and "smart contracts"→developer bigrams added. 3 new strategy patterns (44-46) documented. 15 new tests. Now 1150/1150 routing tests pass.
- Probe pattern 52 (May 2026): shell bare tokens / on-call stop-word drop / license dead zone / e2e collision. "zsh" and "bash" fired raw_first — Fish Shell was mapped but Zsh/Bash weren't; peer-tool audit missed them. Fix: "zsh"→cli, "bash"→cli. "on call" (spaced) fired raw_first "call" — "on" is a stop word leaving bare "call" unmapped; "on-call" (hyphenated) was already mapped. Fix: "call"→monitoring. "license" bare token unmapped — "license checker", "open source license scanner" (after stripping "open"+"source" stop words) fired raw_first; FOSSA unmapped. Fix: "license"→security, "fossa"→security. "e2e encryption" mis-routed to testing via "e2e"→testing — bigrams "e2e encryption"/"e2e encrypted"→security added to override. 4 new strategy patterns (40-43) documented. 16 new tests. Now 1135/1135 routing tests pass.
- Probe pattern 51 (May 2026): LLM eval short-form / self-hosted dual raw_first / changesets mis-category dead zones. "llm eval" (short form) mis-routed to ai via "llm"→ai — "llm evaluation" bigram existed but "llm eval" didn't; similarly "llm benchmarking" (gerund) was missing while "llm benchmark" existed. "ai evaluation" full-word bigram missing — "ai eval" existed but "ai evaluation" didn't. "evals benchmark" mis-routed to ai via bare "evals"→ai. Pattern 38 (short-form/gerund gaps): always add short forms and gerunds alongside full bigrams. "self hosted" / "self-hosted" / "self host" fired raw_first — both tokens ("self", "hosted") are individually unmapped, a Pattern 23 dual dead zone; mapped all forms to devops. "changesets" was wrongly mapped to "devops" — Changesets is a JS monorepo versioning tool (npmjs.com/changesets), not a DevOps automation tool; fixed to "developer". 18 new tests added. Now 1119/1119 routing tests pass.
- Probe pattern 50 (May 2026): favicon / OG / PII / team-messaging / syslog / HMAC dead zones. "favicon" bare token unmapped — favicon tools (favicon.io, RealFaviconGenerator) fired raw_first. "meta tag"/"meta tags" bigrams missing — "meta"→ai fired (wrong; meta tags are SEO). "open graph" bigram intentionally NOT added — "open" is in _FTS_STOP_WORDS; use "og" abbreviation which already maps to seo. "graph ql" spaced form mis-routed to database via bare "graph"→database — "graphql" compound was mapped but the space-separated form wasn't; bigram added → api. "syslog"/"rsyslog" unmapped — syslog protocol tools fired raw_first; both map to logging. "pii" bare token + "pii detection/redaction", "data masking", "data anonymization", "data residency", "data sovereignty" bigrams all unmapped → raw_first; all mapped to security. "hmac" unmapped → security; "request signing" bigram → security. "team messaging" bigram → developer (Mattermost, Rocket.Chat, Zulip). "matrix protocol" bigram → social (Element, Synapse; overrides "protocol"→mcp). 25 new tests added. Now 1101/1101 routing tests pass.
- Probe pattern 49 (May 2026): social-auth / realtime-sync / github-oauth / edge-caching dead zones. "social authentication" was missing as spaced bigram (social login/auth/sign/oauth existed but full word form did not). "realtime sync" routed to api via bare "realtime"→api — local-first/sync DB tools (ElectricSQL, PowerSync, InstantDB) should route to Database. "github oauth"/"github sso"/"github login" routed to devops via "github"→devops — GitHub as OAuth provider → Authentication. "edge caching"/"edge cache" routed to devops via "edge"→devops — edge KV stores (Upstash, Cloudflare KV) → Caching. Regression guards: "social media scheduling"→social, "realtime collaboration"→api, "github actions ci"→devops, "edge database sqlite"→database all intact. 13 new tests added. Now 1076/1076 routing tests pass.
- Probe pattern 48 (May 2026): "event X" analytics dead zones. "event analytics"/"event capture"/"product events" mis-routed to Message Queue via bare "event"/"events"→message. Product analytics tools (PostHog, Mixpanel, Heap) should route to Analytics & Metrics. "event tracking" can't use a bigram — "tracking" is in _FTS_STOP_WORDS and gets stripped. Fix: bigrams "event analytics","event capture"→analytics override "event"→message at position i=0; "product events"→analytics overrides "events"→message at i=1. Regression guards: "event streaming kafka" and "event driven architecture" still correctly route to Message Queue. 5 new tests added.
- Probe pattern 46 (May 2026): localization/consent/build-tool/flux dead zones. "locize" and "lokalise" fired raw_first (crowdin/weblate already mapped; missing tool synonyms). "cookiebot", "osano", "onetrust", "usercentrics" fired raw_first — GDPR consent management tools belong in Security (consent/privacy bare tokens already mapped, but named tools were missing). "grunt" and "gulp" fired raw_first — JS task/build runners belong in Frontend (same category as webpack/esbuild). "flux cd" and "flux gitops" mis-routed to ai via bare "flux"→ai (FLUX.1 image model) — FluxCD is a CNCF GitOps operator, needs bigrams to override. "code formatting tool" fired raw_first via bare "code" (formatter/linter tools belong in Testing; bare "code" has no synonym and bigram "code formatting" was missing). Fixed: bare tokens "locize","lokalise","phrase","transifex"→localization; "cookiebot","osano","onetrust","usercentrics"→security; "grunt","gulp"→frontend; bigrams "flux cd","flux gitops"→devops; bigrams "code formatting","code format"→testing. Regressions guarded: bare "flux" without qualifier still routes to ai (ambiguous). Now 1048/1048 routing tests pass.
- Space-separated compound keys (e.g. `"load balancer": "devops"`) ARE supported — routing checks bigrams (adjacent token pairs) BEFORE individual tokens at each position. Prefer a compound entry when a single token would collide with a different category. After adding, run `scripts/test_search_routing.py` (2115 tests as of May 2026 — check output for current count). Also run `scripts/validate_synonyms.py` to catch duplicate keys (Python dicts silently override with last value) and dead 3-token keys (router only builds bigrams — 3-token compound keys can never fire). The test file docstring contains 32 probe patterns for finding routing gaps systematically. Probe pattern 34 (May 2026): PDF generation + QR code dead zones — "pdf generation"/"pdf generator"/"pdf creator" were routing to file-management via bare "pdf"→file; "qr code generator" was routing to ai-dev via "code generator" bigram firing before bare "qr"→developer. Fixed: bigrams "pdf generation","pdf generator","pdf creator"→developer; "html pdf"→developer (for "html to pdf" after stop-word "to" stripping); "qr code"→developer. Bare "pdf"→file unaffected. Probe pattern 32 (May 2026): "html X" mis-routing — "html"→frontend fired before "parser"→developer and "scraper"→developer, mis-routing html parsing/scraping queries to Frontend Frameworks. Also "html sanitizer" mis-routed to frontend; "sanitizer"→security could never fire when html was the first token. Fixed: "html parser"/"html parsing"→developer, "html scraper"/"html scraping"→developer, "html sanitizer"→security. Known dead zone documented: framework "vs" comparison queries ("react vs vue", "nextjs vs remix") hit raw_first "vs" since both tokens are in _FRAMEWORK_QUERY_TERMS — no safe single-token fix. Probe pattern 31 (May 2026): security dead zones — "penetration testing"→testing (wrong), "dependency scanning"→developer (wrong), "iast" unmapped, "git secrets"→devops (wrong). Fixed with bigrams: "penetration testing/test"→security, "dependency scanning/check/vulnerability"→security, "composition analysis/scanning"→security (note: "software" is a stop word so "software composition" must use post-strip bigrams), "iast"→security, "git secrets/secret"→security. Probe pattern 30 note: bigrams that include _FTS_STOP_WORDS tokens (framework, tool, service, etc.) can NEVER fire — always verify both tokens survive stop-word stripping before adding. Example: "web framework" is impossible; "web server" is possible since "server" is not a stop word.
- IMPORTANT: bigrams that include a token from `_FTS_STOP_WORDS` can NEVER fire — stop words are stripped before bigram matching. For example `"micro service"` will never match because "service" is a stop word. Always check if both tokens survive stop-word stripping before adding a bigram entry.
- 3-token compound keys (e.g. `"ai load balancer"`) can NEVER fire — the router only builds 2-token bigrams from adjacent token pairs. If a target term has 3 words, either map the first 2 tokens or use a single-token fallback.
- After bulk tool updates (tags, categories, install commands): always rebuild FTS: `INSERT INTO tools_fts(tools_fts) VALUES('rebuild')` + `PRAGMA wal_checkpoint(TRUNCATE)`.
- Probe pattern 35 (May 2026): UX research dead zones — "user research"/"user interview" fired raw_first because "user" has no mapping. "maze", "usertesting", "lookback", "dovetail" were also unmapped. Fixed: bigrams "user research"/"user interview"→feedback; bare tokens "qualitative","maze","usertesting","lookback","dovetail"→feedback. Regressions guarded: "user authentication"→auth, "user analytics"→analytics unaffected. Now 846/846 routing tests pass.
- Probe pattern 37 (May 2026): SaaS metrics + product feedback dead zones — "mrr"/"arr"/"cac"/"revenue" had no mapping → raw_first fired. "feature request" mis-routed via "feature"→feature-flags. "release notes" widget queries mis-routed via "release"→devops. Fixed: bare "mrr","arr","cac","revenue"→analytics; bigrams "feature request","feature requests"→feedback; "release notes"→feedback. Also removed pre-existing msgpack duplicate. Regressions guarded: "feature flag toggle"→feature-flags, "release version management"→devops. Now 877/877 routing tests pass.
- Probe pattern 38 (May 2026): typography / versioning / modal-dialog / contrast / cost / sortable / semantic-release dead zones. "typography","versioning","contrast","cost","sortable" had no mapping → raw_first. "modal dialog"→ai (Modal.com collision). "semantic release"→search (semantic vector-search collision). "screen reader","keyboard navigation","focus management" fired wrong bare tokens. Fixed: bare tokens "typography"→frontend, "versioning"→devops, "contrast"→testing, "cost"→devops, "sortable"→frontend; bigrams "modal dialog"→frontend, "semantic release"→devops, "screen reader"→testing, "keyboard navigation"→testing, "focus management"→frontend. Now 902/902 routing tests pass.
- Probe pattern 39 (May 2026): zero-trust / reactive / hallucination / data-quality / schema-registry dead zones. "zero trust" (spaced) had no bigram — "zero trust network X" mis-routed via "network"→monitoring (should be security). "ztna" abbreviation unmapped. "reactive" bare token unmapped — RxJS/MobX queries fired raw_first. "hallucination" unmapped — Guardrails AI/RAGAS detection tools unreachable. "data quality" bigram missing — "quality"→testing fired (wrong; Monte Carlo/Soda → analytics). "schema registry" bigram missing — "schema"→developer fired (wrong; Confluent/Karapace → message). Fixed: bigram "zero trust"→security, "ztna"→security, "reactive"→frontend, "hallucination"→ai, bigram "data quality"→analytics, bigram "schema registry"→message. Now 920/920 routing tests pass.
- Probe pattern 40 (May 2026): code quality / accessibility testing dead zones. "complexity" bare token unmapped — "cyclomatic complexity", "code complexity analyzer" fired raw_first (SonarQube, CodeClimate, Lizard unreachable). "axe" (Deque axe-core) unmapped — accessibility testing library returned no category. "a11y testing"/"a11y test" bigrams missing — "a11y"→frontend fired for testing-specific queries. "wcag" bare token missing — "wcag 2.1 compliance" mis-routed via "compliance"→security (WCAG is accessibility testing, not security). "tech debt" bigram missing — both tokens fired raw_first. "dead code" bigram missing — Knip/ts-prune queries returned no category. Fixed: "complexity"→testing, "axe"→testing, bigrams "a11y testing"/"a11y test"→testing, "wcag"→testing + "wcag compliance"→testing bigram (overrides compliance→security), "tech debt"→developer, "dead code"→testing. Regressions guarded: "password complexity"→security, "accessibility library"→frontend, "soc2 compliance"→security, "a11y linting"→frontend all unaffected. Now 937/937 routing tests pass.
- Probe pattern 41 (May 2026): bundle analysis / project management dead zones. "bundle" bare token unmapped — "bundle size analyzer", "bundle stats", "bundle budget" fired raw_first (webpack-bundle-analyzer, Bundlephobia unreachable). "bug" bare token unmapped — "bug tracker", "bug tracking tool" fired raw_first (Linear, Plane unreachable). "retro"/"retrospective" unmapped — retrospective tooling (TeamRetro, EasyRetro) fired raw_first. "okr" unmapped — OKR tools (Lattice, Perdoo, 15Five) fired raw_first. "standup" unmapped — async standup bots (Geekbot, Standuply) fired raw_first. "issue tracker" bigram missing — Linear/Plane unreachable for bare "issue tracker" queries. Fixed: "bundle"→frontend, "bug"→project, "retro"→project, "retrospective"→project, "okr"→project, "standup"→project, bigram "issue tracker"→project. NOTE: "issue tracking" bigram intentionally skipped — "tracking" is in _FTS_STOP_WORDS. "knowledge base" spaced bigram intentionally skipped (see db.py note ~6381) — "knowledge base llm/chatbot" must route to AI. Regressions guarded: "debugging tool"→developer, "webpack bundle analyzer"→frontend, "sprint planning"→project unaffected. Now 954/954 routing tests pass.
- Probe pattern 42 (May 2026): DAST / memory-profiler / image-scanning dead zones. "dynamic analysis" fired raw_first — OWASP ZAP, Burp Suite, Nuclei unreachable for generic DAST queries (bare "dast" was mapped but the spelled-out phrase wasn't). "memory profiler"/"memory profiling" mis-routed to caching via bare "memory"→caching firing before "profiler"→monitoring — token-order bug; bigrams now take precedence. "image scanning"/"image scan" mis-routed to media via "image"→media (wrong; Trivy, Grype, Snyk Container live in security). Fixed: bigram "dynamic analysis"→security; bigrams "memory profiler"/"memory profiling"→monitoring (override memory→caching); bigrams "image scanning"/"image scan"→security (override image→media). Regressions guarded: bare "memory store redis"→caching, "in memory cache"→caching, "dynamic import javascript"→frontend all unaffected. Now 963/963 routing tests pass.
- Probe pattern 101 (autonomous loop, May 2026): XAI / ML fairness / federated learning dead zones. "explainability library" stripped "library"→stop-word → raw_first "explainability" (unmapped; LIME, SHAP, Captum are XAI tools → AI Standards & Specs); bare "explainability"→"ai standards" + bare "interpretability"→"ai standards" + bare "fairness"→"ai standards" added. Bigrams "explainable ai"→"ai standards", "ai fairness"→"ai standards", "ai explainability"→"ai standards" added for phrase queries. Named XAI tools all fired raw_first: bare "shap"→ai (SHapley Additive exPlanations, 22k★), "captum"→ai (PyTorch attribution by Meta, 5k★), "eli5"→ai (ML explainability for sklearn, 2.7k★), "fairlearn"→ai (Microsoft ML fairness, 1.9k★), "aif360"→ai (IBM AI Fairness 360, 2.4k★), "aequitas"→ai (UChicago fairness auditing), "flwr"→ai (Flower Federated Learning Python pkg, 5.8k★), "pysyft"→ai (privacy-preserving ML, 9.5k★), "opendp"→ai (OpenDP Harvard differential privacy). "federated learning" bigram→ai added — overrides bare "federated"→authentication (identity federation) for ML federation queries; regression guard: "federated identity"/"federated sso" still route via bare "federated"→authentication. Probed but no fix needed: "model interpretability"→ai (correct via "model"→ai at pos 0), "shap values python"→ai (correct after fix). 22 new tests + 2 regression guards. Now 2035/2035 routing tests pass.
- Probe pattern 102 (autonomous loop, May 2026): Distributed consensus / probabilistic data structures / spatial indexing / deepfake dead zones. "raft consensus"/"consensus algorithm"/"paxos vs raft" all fired raw_first — Raft/Paxos consensus protocol tools (etcd, HashiCorp Raft, CockroachDB, Apache ZooKeeper) belong in Database; bare "raft"→database, bare "consensus"→database, bare "paxos"→database + bigram "consensus algorithm"→database added. "materialized view postgres"/"materialized table refresh" fired raw_first — materialized views are a database optimization concept; bare "materialized"→database + bigram "materialized view"→database added. Probabilistic data structure queries all fired raw_first: "bloom filter" → bigram "bloom filter"→developer (rbush, bloomfilter.js); bare "hyperloglog"→developer (Redis HLL, cardinality estimation); bare "trie"→developer (prefix tree — trie.js, fast-trie); bare "bitset"→developer (bitmap ops — bitset.js, RoaringBitmap); bare "roaring"→developer (Roaring Bitmaps compressed bitset). Spatial dead zones: bare "spatial"→maps (spatial index libraries — rbush, flatbush); bare "geohash"→maps (geospatial encoding — geohash.js); bare "rtree"→maps (R-tree — rbush, flatbush); bigram "spatial index"→maps. AI/security dead zones: bare "deepfake"→ai + bigram "deepfake detection"→ai (deepfake detection APIs — Sensity, Hive Moderation, DeepFaceLab); bare "watermarking"→security, bare "watermark"→security (digital watermarking — invisible-watermark, C2PA); bare "steganography"→security (LSB steganography, stegano). Regression guard: "etcd raft based"→devops (etcd already mapped→devops, takes priority over "raft"→database). 21 new tests + 1 regression guard. Now 2056/2056 routing tests pass.
- Probe pattern 103 (autonomous loop, May 2026): Repo-to-LLM dead zones. "repo to llm"/"codebase to llm"/"repository to llm" all fired raw_first — stop-word removal turns these into "repo llm"/"codebase llm"/"repository llm" bigrams respectively; tools are Repomix, gitingest, repo-to-text which live in AI Dev Tools. Bigrams "repo llm"/"codebase llm"/"repository llm"→"ai dev" added. "repo ingest"/"repo ingestion" fired raw_first — bare "ingestion" was already mapped to Background Jobs; bigrams "repo ingest"→"ai dev" and "repo ingestion"→"ai dev" added to override the bare-token Background match. "code ingest"/"code ingestion"/"codebase ingest"/"codebase ingestion" added similarly. Regression guard: "data ingestion pipeline" still routes to Background Jobs (bare "data"→database doesn't fire before "data ingestion" bigram... wait, actually "data ingestion" bigram →background exists and fires first). 12 new tests. 2066/2066 pass.
- Probe pattern 104 (autonomous loop, May 2026): SOAP / XML-RPC / ATO / multi-tenancy / optimistic-UI / stale-cache / Cloudflare-Worker dead zones. "soap client"/"soap wsdl"/"soap service" all fired raw_first — SOAP web service client libraries (Zeep, node-soap, Apache CXF, SoapUI) belong in API Tools; bare "soap"→api + bare "wsdl"→api added. "xml rpc server"/"xml-rpc client" fired raw_first / misfired to Developer via bare "xml"→developer — XML-RPC protocol tools belong in API Tools; bigram "xml rpc"→api + hyphenated "xml-rpc"→api added. "account takeover protection"/"account takeover tool" fired raw_first "account" — ATO prevention tools (Arcjet, BotD, Plaid Shield) belong in Security; bigram "account takeover"→security added. "multi-tenant saas"/"multi-tenant app" fired raw_first — bare "tenant" and "multitenant" already mapped→authentication, but hyphenated "multi-tenant" was missing; bare "multi-tenant"→authentication added. "optimistic update"/"optimistic ui" fired raw_first — optimistic UI updates (React Query, SWR, Zustand) are a Frontend Frameworks pattern; bare "optimistic"→frontend added. "stale while revalidate"/"stale cache invalidation" fired raw_first — "service" is a stop-word so "stale while revalidate" → ["stale","revalidate"] bigram; bare "stale"→caching + bigram "stale revalidate"→caching added. "cf worker wrangler"/"cf worker alternative" misfired to Background Jobs via bare "worker"→background — "cf" prefix means Cloudflare Workers (devops); bigram "cf worker"→devops added ("cf workers" plural was already correct via "workers"→devops). "message signing library"/"message authentication code" misfired to Message Queue via bare "message"→message — message authentication/signing is a Security concept; bigrams "message signing"→security + "message authentication"→security added. "dependency audit tool"/"package vulnerability scanner" misfired to Developer — supply-chain security tools (Snyk, npm audit, OWASP Dependency-Check) belong in Security; bigrams "dependency audit"→security + "package vulnerability"→security added. Regression guards: "cf workers deploy"→devops, "background worker queue"→background, "multi tenant saas"→auth, "multitenant architecture"→auth all unchanged. 23 new tests + 6 regression guards. 2089/2089 pass.
- Probe pattern 106 (autonomous loop, May 2026): DAG-workflow / LLM-load-balancer / AI-cost / a/b-testing / image-embedding dead zones. "dag scheduler"/"dag runner"/"dag workflow" misfired to raw_first or Developer via bare "runner"→developer (wrong; DAG-based orchestrators Airflow, Prefect, Dagster belong in Background Jobs); bare "dag"→background + bigram "dag runner"→background added. "llm load balancer" misfired to DevOps via bigram "load balancer"→devops firing at pos 1 (wrong; LiteLLM, PortKey.ai LLM load balancing belongs in AI & Automation); bigram "llm load"→ai added at pos 0 (fires before "load balancer"→devops). "ai cost optimization"/"ai cost tracking" misfired to DevOps via bare "cost"→devops ("ai" is not in _CAT_SYNONYMS so bare "cost" wins; wrong; Helicone, PortKey.ai AI cost tools belong in AI & Automation); bigram "ai cost"→ai added. "a/b testing" misfired to Testing via bare "testing" (slash normalization: "a/b" kept as single token, "ab"→feature worked but "a/b" slash form was not mapped); bare "a/b"→feature + bigram "a/b testing"→feature added. "image embedding model"/"image embedding search" misfired to Media Server via bare "image"→media firing before "embedding"→ai (wrong; CLIP, Jina AI, Cohere image embedding belong in AI & Automation); bigram "image embedding"→ai added (fires before bare "image"). Regressions guarded: "ab testing tool"→feature, "cost optimization infra"→devops, "load balancer nginx"→devops all unchanged. 14 new tests + 3 regression guards. 2115/2115 pass.
- Probe pattern 105 (autonomous loop, May 2026): LLM token-budget / json-mode / context-compression / long-context dead zones. "token budget management"/"token budget tracker" misfired to Authentication via bare "token"→authentication (wrong; LLM token budget tools Helicone/LangSmith/OpenMeter belong in AI); bigrams "token budget"→ai + "token quota"→ai added (complement to existing "token limit"→ai and "token pricing"→ai). "json mode openai"/"json mode llm" misfired to Developer Tools via bare "json"→developer (wrong; LLM JSON/structured-output libraries Instructor, Outlines, Marvin, Jsonformer belong in AI); bigram "json mode"→ai added (fires before bare "json"→developer). "context compression llm"/"prompt context compression" misfired to Frontend Frameworks via bare "context"→frontend (wrong; LLM context compression tools LLMLingua, Selective Context, LongLLMLingua belong in AI); bigram "context compression"→ai added. "long context model processing"/"long context llm" misfired to Frontend via "context"→frontend at pos 1 (wrong; long-context LLM processing belongs in AI); bigram "long context"→ai added. Regressions guarded: "token limit gpt4"→ai, "token pricing comparison"→ai, "context window limit"→ai all unchanged. 12 new tests + 3 regression guards. 2101/2101 pass.
- Probe pattern 107 (autonomous loop, May 2026): AI-standards / benchmark-LLM / vector-cache dead zones. "ai standards nist"/"ai standards framework" misfired to raw_first — "ai" is not in _CAT_SYNONYMS so bare "ai" gives no category boost; AI standards tools (NIST AI RMF, ModelCards, Guardrails) belong in AI Standards & Specs; bigram "ai standards"→"ai standards" added. "benchmark llm"/"llm benchmark" misfired to Testing via bare "benchmark"→testing (wrong; LLM evaluation tools lm-eval-harness, evalplus, BIG-bench belong in AI Standards); bigrams "benchmark llm"→"ai standards" + "llm benchmark"→"ai standards" added. "vector cache embeddings"/"vector cache redis" misfired to Database via bare "vector"→database (wrong; vector-level semantic caching tools GPTCache, Redis semantic-cache belong in Caching); bigram "vector cache"→caching added (fires before bare "vector"→database). Regressions: "benchmark tool k6"→testing, "vector database pinecone"→database unchanged. 10 new tests + 4 regression guards. 2125/2125 pass.
- Probe pattern 108 (autonomous loop, May 2026): LitElement / BPMN / visual-builder / custom-elements dead zones. "litelement"/"litjs" compound slug forms were raw_first — bare "lit"→frontend already existed; compound forms now added bare "litelement"→frontend + bare "litjs"→frontend. "bpmn"/"bpmn tool"/"bpmn engine" were raw_first — Camunda/Zeebe individually mapped but generic BPMN acronym missing; bare "bpmn"→background added. "visual builder"/"visual app builder" misfired to Testing via bare "visual"→testing (wrong; visual drag-and-drop builders Plasmic, Builder.io, Puck belong in Frontend); bigram "visual builder"→frontend added; NOTE: "app" is in _FTS_STOP_WORDS so "visual app builder" strips to ["visual","builder"] — bigram fires correctly. "visual database"/"visual database browser"/"visual database tool" misfired to Testing via bare "visual"→testing (wrong; visual DB managers TablePlus, DBngin, Beekeeper belong in Database); bigram "visual database"→database added. "custom elements"/"custom elements web" were raw_first — "webcomponent"/"webcomponents" already mapped; spaced "custom elements" form missing; bigram "custom elements"→frontend added. Regressions guarded: "visual testing"→testing, "visual regression"→testing, "visual workflow builder"→background, "visual editor"→frontend all unchanged. 21 new tests (14 fixes + 7 regression guards). 2146/2146 pass.
- Probe pattern 138 (autonomous loop, May 2026): data-contract / post-quantum / cryptography dead zones. "data contract validation"/"data contract soda" → testing (wrong; "contract"→testing fires; data contracts in data engineering = schema agreements → Analytics); bigram "data contract"→analytics added (fires before "contract"→testing). "post quantum cryptography" → raw_first (all tokens unmapped; CRYSTALS-Kyber, liboqs, NIST PQC tools → Security); bigram "post quantum"→security added. "pqc library"→raw_first (PQC abbreviation unmapped); bare "pqc"→security added. "cryptography library"→raw_first ("cryptography" missing, only "crypto"→security existed; Python cryptography, libsodium, Tink → Security); bare "cryptography"→security added. Regressions guarded: "contract testing pact"→testing, "api contract"→testing, "crypto library"→security, "quantum safe encryption"→security all unchanged. 4 new entries. 14 new tests + 5 regression guards. 2691/2691 pass.
- Probe pattern 137 (autonomous loop, May 2026): embedded/IoT RTOS / iOS-dependency dead zones. "rtos library"/"freertos alternative" → raw_first — FreeRTOS, Zephyr RTOS, mbed OS, NuttX belong in Developer Tools; bare "rtos"/"freertos"/"zephyr"/"arduino"/"platformio"/"micropython"/"yocto"/"cocoapods"/"esp32"/"esp8266"→developer added. "embedded linux tool" → database (wrong; "embedded"→database collision for Linux context); bigrams "embedded linux"/"embedded system"→developer added (bare "embedded"→database unchanged for "embedded database" queries). "raspberry pi sdk" → api (wrong; meaningful=["raspberry","pi","sdk"], "sdk"→api fires); bigram "raspberry pi"→developer added. NOTE: "zephyr"→developer may also route ZephyrScale (test management tool) to developer — acceptable since ZephyrScale is enterprise Jira plugin unlikely in indie catalog. 13 new entries. 20 new tests + 3 regression guards. 2677/2677 pass.
- Probe pattern 135 (autonomous loop, May 2026): ANN / shell-completion / ECS named-tool dead zones.
- Probe pattern 136 (autonomous loop, May 2026): ZKP / PBT / distributed-tx / credential-rotation dead zones. "pbt framework" fired raw_first — Property-Based Testing abbreviation (Hypothesis, fast-check, Proptest); bare "pbt"→testing added. "zero knowledge proof" fired raw_first — ZKP crypto libraries (Circom, SnarkJS, Halo2, Arkworks) belong in Security; bigrams "zero knowledge"/"zk snark"/"zk stark"→security + bare "zkp"/"zk"/"snark"/"stark"/"circom"/"snarkjs"/"arkworks"/"halo2"→security added. "two phase commit protocol" misfired to MCP via bare "protocol"→mcp (wrong; 2PC is a distributed DB transaction pattern); bigram "two phase"→database added (fires at pre-pass i=0 before "protocol"→mcp). "distributed transaction management" misfired to project via "management"→project (Atomikos, Seata, Narayana → Database); bigram "distributed transaction"→database added. "credential rotation vault" misfired to authentication via "credential"→auth (wrong; credential rotation is a Security hygiene pattern); bigram "credential rotation"→security added. "service account rotation" fired raw_first (meaningful=["account","rotation"] after stop-word "service" stripped; neither token mapped); bigram "account rotation"→security added. Regressions guarded: "property based testing"→testing, "two factor auth"→authentication, "zero downtime deploy"→devops, "model context protocol"→mcp, "protocol buffer grpc"→api all unchanged. 30 new tests. Now 2657/2657 routing tests pass. "ann library python" fired raw_first — ANN (Approximate Nearest Neighbor) index libraries (FAISS, Annoy, HNSWlib, ScaNN) belong in Search Engines (complement to "vector"→database for full vector DBs); bare "ann"→search + bigram "nearest neighbor"→search added. "tab completion bash"/"tab completion zsh" fired raw_first for both "tab" and "completion" — shell tab completion tools (fig, carapace, zsh-completions) belong in CLI Tools; bigram "tab completion"→cli added. "flecs ecs" mis-routed to devops via bare "ecs"→devops (AWS ECS collision) — Flecs is the most popular standalone C/C++ ECS library for game dev (6k★); bare "flecs"→games added. Regressions guarded: "code completion"→ai-dev via bigram unchanged. 4 new entries. 6 new tests + 2 regression guards. 2627/2627 pass.
- Probe pattern 134 (autonomous loop, May 2026): policy-engine named tools / game-primitives / compiler-tools / parser-grammar / networking dead zones. "kyverno alternative" fired raw_first — Kyverno is a Kubernetes-native policy engine → DevOps; bare "kyverno"→devops added (complement to probe 133's "policy"→devops). "voxel engine" fired raw_first — voxel world generators (Veloren, Minetest) belong in Games; bare "voxel"→games added. "antlr alternative"/"antlr grammar" fired raw_first — ANTLR is a parser generator → Developer Tools (complement to "dsl"→developer from probe 133); bare "antlr"→developer added. "grammar definition"/"grammar parser" fired raw_first — PEG/EBNF grammar tooling (PEG.js, Nearley, lark) belongs in Developer Tools; bare "grammar"→developer added (NOTE: "grammar checker" for writing tools also routes to developer — acceptable for dev catalog). "raycast extension"/"raycast alternative" fired raw_first — Raycast.com is a macOS developer launcher/productivity tool; bare "raycast"→developer added. "sprite sheet generator" fired raw_first "sprite" — 2D sprite tools (TexturePacker, Free Texture Packer) belong in Games; bare "sprite"→games added (regression guard: "css sprites"→frontend via "css" unchanged). "linker tool" fired raw_first "linker" — linkers (mold, LLD, Gold, GNU ld) belong in Developer Tools; bare "linker"→developer added. "udp server library" fired raw_first "udp" — UDP networking libraries belong in API Tools (complement to "socket"→api); bare "udp"→api added. "entity component system" mis-routed to Database via bare "entity"→database — ECS game architecture tools (Flecs, bevy_ecs, EnTT) belong in Games; bigram "entity component"→games added (regression guards: "entity framework"→database via bare "entity", "entity extraction"→ai via bigram, both unchanged). 9 new entries. 13 new tests + 4 regression guards. 2621/2621 pass.
- Probe pattern 133 (autonomous loop, May 2026): NLP lemmatization / screen-tools / AST / game-physics / policy-as-code / DSL / security-research dead zones. "lemmatization nltk"/"text lemmatizer" fired raw_first — NLP lemmatizer tools (spaCy, NLTK, TextBlob) belong in Developer Tools (consistent with stemmer→developer from probe 132); bare "lemmatize"/"lemmatizer"/"lemmatization"→developer added. "text normalizer"/"text normalization library" fired raw_first — text pre-processing utilities (python-ftfy, Unidecode) belong in Developer Tools; bare "normalizer"→developer + bigrams "text normalization"/"text normalizer"→developer added. "screen capture tool"/"screen share sdk" fired raw_first "screen" — screen utilities (ShareX, Flameshot, Screen.so) are Developer Tools; bare "screen"→developer added (regression guards: "screen recording" bigram→analytics and "screen reader" bigram→testing fire in pre-pass first, unchanged). "abstract syntax tree"/"abstract class" fired raw_first "abstract" — AST tools (tree-sitter, @babel/parser, espree) are Developer Tools; bare "abstract"→developer + bigram "syntax tree"→developer added. "physics engine"/"physics simulation" fired raw_first — Rapier, Box2D, Matter.js, Cannon.js belong in Games & Entertainment; bare "physics"→games + bare "pathfinding"→games + bare "collision"→games + bigram "collision detection"→games added. "policy generator"/"policy as code" fired raw_first "policy" — OPA, Conftest, Kyverno, Cedar belong in DevOps; bare "policy"→devops added (regression guards: "privacy policy"→security via "privacy", "cookie policy"→auth via "cookie" both unchanged). "dsl language" misfired to devops via "domain"→devops — ANTLR, Langium, Chevrotain are Developer Tools; bare "dsl"→developer added. "jar decompiler"/"bytecode analysis"/"disassembler" fired raw_first — Jadx, Ghidra, JD-GUI, Binary Ninja are Security Tools; bare "decompiler"/"bytecode"/"disassembler"→security + bigram "binary analysis"→security added. 24 new tests + 7 regression guards. 2608/2608 pass.
- Probe pattern 132 (autonomous loop, May 2026): browser-extension/event-loop/coroutine/pipeline/codegen dead zones + 4 duplicate cleanup. Removed 4 duplicate _CAT_SYNONYMS keys (all same-value dupes flagged by validate_synonyms.py): "llm benchmark" (first at 7747, dupe at 7977), "crypto payment" (first at 3382, dupe at 8990), "dependency check" (first at 8462, dupe at 9797), "fastcheck" (first at 6027, dupe at 9984). New fixes: bare "firefox"→developer (complement to "chrome"→developer; covers "firefox addon", "firefox extension"); bare "addon"/"addons"→developer (browser addons, VS Code addons); bigram "event loop"→developer overrides "event"→message (JS event loop tools clinic.js/libuv); bigram "async runtime"→developer (Tokio, asyncio runtimes — new dead zone); bare "coroutine"/"coroutines"→developer (gevent, greenlet, Python coroutines); bigram "kotlin coroutines"→developer (overrides "kotlin"→api; Kotlin Coroutines library); bare "stemmer"→developer (NLTK stemmer, Snowball — NLP text processing utilities); bigram "pipeline runner"→devops (GitLab Runner, GitHub Actions Runner — overrides "pipeline"→background); bigram "typescript codegen"→developer (ts-to-zod, quicktype — overrides "typescript"→frontend); bigram "speculative decoding"→ai (LLM inference optimization — overrides "decoding"→developer); bigrams "catalog backstage"/"catalog idp"→developer (Backstage IDP service catalog — after "service" stop-word strip, "catalog"→devops was winning). Regressions guarded: "event sourcing"→message, bare "pipeline"→background, "typescript"→frontend, "kotlin"→api (without coroutines suffix) all unchanged. 21 new tests + 5 regression guards. 2581/2581 pass.
- Probe pattern 131 (autonomous loop, May 2026): web-automation / dependency-scanner misfires. "web automation tool/python" misfired to AI & Automation via bare "automation"→ai (wrong; web automation tools Playwright, Puppeteer, Selenium scripted for scraping/automation belong in Testing like browser automation); bigram "web automation"→testing added (fires before bare "automation"→ai). "dependency scanner tool" misfired to Developer Tools via bare "dependency"→developer (wrong; dependency security scanners Snyk, OWASP Dependency-Check, npm audit belong in Security; existing "dependency vulnerability"→security bigram didn't cover "scanner" form); bigrams "dependency scanner"→security + "dependency check"→security added. Regressions guarded: bare "automation"→ai unchanged for "automation platform" queries, "workflow automation n8n"→background unchanged (bigram "workflow automation"→background fires correctly), bare "dependency"→developer unchanged for "dependency injection"/"management" queries, bigram "dependency vulnerability"→security unchanged. 12 new tests + 6 regression guards. 2560/2560 pass.
- Probe pattern 130 (autonomous loop, May 2026): ethical-ai / load-shedding dead zones. "ethical ai tool/framework" fired raw_first — both "ethical" and "ai" unmapped (AI ethics/fairness tools Fairlearn, AI Fairness 360 belong in AI Standards); bigrams "ethical ai"→ai standards + "ai ethics"→ai standards added. "ethical hacking" also dead zone — bare "ethical"→security added (pentest context; bigram fires first for ai ethics queries, preventing collision). "load shedding" misfired to Testing via bare "load"→testing (wrong; load shedding is an API resilience pattern like circuit breakers; Envoy/HAProxy/Nginx drop policies); bigram "load shedding"→api added. Regressions guarded: "responsible ai"→ai standards, "ai safety"→ai standards, "load testing k6"→testing, "load balancer nginx"→devops all unchanged. 9 new tests + 4 regression guards. 2550/2550 pass.
- Probe pattern 129 (autonomous loop, May 2026): shared-inbox / ai-routing / mob-programming dead zones. "shared inbox tool/software" misfired to Notifications via bare "inbox"→notifications (wrong; collaborative shared-inbox tools Missive, Front, Help Scout, Hiver are Customer Support); bigrams "shared inbox"→support + "team inbox"→support added. "ai routing litellm/alternative" misfired to Frontend Frameworks via bare "routing"→frontend (wrong; LLM router/proxy tools LiteLLM, PortKey, OpenRouter are AI tooling); bigram "ai routing"→ai added. "llm routing proxy/litellm" also misfired to Frontend via same "routing"→frontend token; bigram "llm routing"→ai dev + "model routing"→ai added. "mob programming tool/software" fired raw_first — both "mob" and "programming" unmapped; mob programming tools (Mob.sh, Mobster) are Developer Tools; bigram "mob programming"→developer added. Regressions guarded: bare "inbox"→notifications unchanged for notification-inbox component queries, "ai gateway"→ai unchanged, "routing library"→frontend unchanged, "pair programming"→ai intentionally unchanged. 16 new tests + 5 regression guards. 2538/2538 pass.
- Probe pattern 128 (autonomous loop, May 2026): binary data-format dead zones. "capnproto library" fired raw_first — Cap'n Proto (zero-copy binary serialization + RPC) belongs in API Tools alongside flatbuffers/msgpack/thrift; bare "capnproto"→api + bare "capnp"→api added. "cbor encoder/decoder" fired raw_first — CBOR (RFC 7049 Concise Binary Object Representation) belongs in API Tools; bare "cbor"→api added. "bson parser" misfired to Developer via "parser"→developer (pos 1) — BSON (MongoDB binary JSON) belongs in Database; bare "bson"→database added (fires at pos 0 before "parser"→developer). "geojson library/parser" fired raw_first — GeoJSON is a geographic data format; tools like Turf.js, MapLibre, leaflet-geojson live in Maps & Location; bare "geojson"→maps added. "ndjson reader" fired raw_first — NDJSON (Newline Delimited JSON) format libraries belong in Developer Tools; bare "ndjson"→developer + "jsonl"→developer + "jsonlines"→developer added. "jsonl format" misfired to Testing via "format"→testing (pos 1) — JSON Lines format belongs in Developer Tools; bare "jsonl"→developer fires at pos 0 overriding "format". "pyarrow dataframe" fired raw_first — PyArrow (Apache Arrow Python bindings) belongs in Database alongside polars/parquet; bare "pyarrow"→database added. "orc file reader" fired raw_first — Apache ORC (columnar storage format) belongs in Database alongside parquet/avro; bare "orc"→database added. Regressions guarded: msgpack/flatbuffers/avro/parquet/polars unchanged; turf→maps unaffected by orc addition. 18 new tests + 6 regression guards. 2522/2522 pass.
- Probe pattern 127 (autonomous loop, May 2026): SLSA/attestation/style-dictionary/JSON-path dead zones. "slsa attestation" fired raw_first — SLSA (Supply-chain Levels for Software Artifacts) is a Google supply-chain security framework; bare "slsa"→security added. "artifact attestation" misfired to DevOps via "artifact"→devops (artifact registry collision); bigram "artifact attestation"→security added as pre-pass override. "attestation" bare token unmapped — build/code-signing attestation belongs in Security; bare "attestation"→security added. "style dictionary tool/amazon" fired raw_first — Amazon Style Dictionary is a design token build system for Frontend; bigram "style dictionary"→frontend added (fires before bare "style" which has no mapping). "jsonata transform/query" fired raw_first — JSONata is a JSON query/transform language → Developer Tools; bare "jsonata"→developer added. "jmespath query" misfired to Database via "query"→database at pos 1 — JMESPath is a JSON path query language → Developer Tools; bare "jmespath"→developer added (fires at pos 0 before "query"→database). "jsonpath library/expression" fired raw_first — JSONPath → Developer Tools; bare "jsonpath"→developer added. 19 new tests + 5 regression guards. 2498/2498 pass.
- Probe pattern 126 (autonomous loop, May 2026): builder/crud/visual-programming dead zones. "app builder platform" misfired to raw_first — "app"+"platform" both stop-words leaving bare "builder" unmapped; bare "builder"→developer added. "crud generator/api" misfired to raw_first — "crud" bare token had no mapping; bare "crud"→developer added. "visual programming language/tool" misfired to Testing via bare "visual"→testing (wrong; Blockly, Node-RED, Scratch belong in Developer Tools); bigram "visual programming"→developer added. Regressions guarded: "page/form/query/site/workflow/visual builder" still route to their correct categories. 17 new tests + 7 regression guards. 2479/2479 pass.
- Probe pattern 125 (autonomous loop, May 2026): Discord/Slack/Telegram SDK dead zones + UI library plural forms. "discord bot/library/framework" fired raw_first — Discord bot SDK libraries (discord.js, discord.py, discordgo) belong in Developer Tools; bare "discord"→developer + bare "discord.js"→developer added. "slack sdk/bot framework" fired raw_first — Slack SDK tools (Bolt, slackclient, slack-go) belong in Developer Tools; bare "slack"→developer added. "telegram bot/library" fired raw_first — Telegram bot libraries (aiogram, python-telegram-bot, Telethon) belong in Developer Tools; bare "telegram"→developer + bare "aiogram"→developer + bare "telethon"→developer added. "tooltips react/library" fired raw_first "tooltips" — plural "tooltip"→frontend existed but plural form "tooltips" was missing; bare "tooltips"→frontend added. "walkthroughs product/app" fired raw_first "walkthroughs" — plural form similarly missing; bare "walkthroughs"→frontend added. "joyride react" fired raw_first "joyride" — React Joyride (product tour library, 6k★) unmapped; bare "joyride"→frontend added. NOTE: "discord webhook"/"slack webhook" → "discord"/"slack"→developer fires at pos 0, winning over "webhook"→api at pos 1; regression guard documents this is correct behavior. 23 new tests + 8 regression guards. 2461/2461 pass.
- Probe pattern 124 (autonomous loop, May 2026): computer vision / geospatial-DB / IoT dead zones. "face detection library"/"face recognition python" fired raw_first "face" — bare "face" was unmapped; CV face detection tools (face-api.js, OpenCV, AWS Rekognition, DeepFace) belong in AI; bare "face"→ai + bigrams "face detection"→ai + "face recognition"→ai added. "object detection model/python" misfired to File Management via bare "object"→file (object storage collision; YOLOv8/OpenCV/torchvision detection → AI); bigram "object detection"→ai added. "semantic segmentation pytorch" misfired to Search Engine via bare "semantic"→search (semantic search collision; segmentation is a CV task → AI); bigram "semantic segmentation"→ai added. "image classification tensorflow" and "image recognition model" misfired to Media Server via bare "image"→media (image hosting collision; classification/recognition → AI); bigrams "image classification"→ai + "image recognition"→ai + "image segmentation"→ai added. "yolo detection/alternative" and "mediapipe alternative" fired raw_first — ML-framework-specific tokens unmapped; bare "yolo"→ai + bare "mediapipe"→ai added. "pose estimation python" fired raw_first — bare "pose" unmapped; bare "pose"→ai + bigram "pose estimation"→ai added. "depth estimation monocular" fired raw_first — bare "depth" unmapped; bigram "depth estimation"→ai added. "postgis alternative/extension" fired raw_first — bare "postgis" unmapped; PostGIS is the PostgreSQL spatial extension → Database; bare "postgis"→database added. "iot platform" fired raw_first — bare "iot" unmapped; IoT platforms belong in DevOps & Infrastructure; bare "iot"→devops added. Regressions guarded: "object storage s3"→file, "semantic search api"→search, "image upload react"→frontend, "image optimization cdn"→file all unchanged. 25 new tests. 2438/2438 pass.
- Probe pattern 123 (autonomous loop, May 2026): headless-table / virtualized-list dead zones. "headless table react"/"headless table component" mis-routed to CMS via bare "headless"→cms (wrong; TanStack Table, React Table, AG Grid headless mode are Frontend Frameworks UI components); bigrams "headless table"→frontend + "headless datagrid"→frontend added before bare "headless"→cms fires. "datagrid" alone already maps→frontend but bare "headless" at position 0 in pre-pass fired cms before the correct datagrid token. "virtualized list react" fired raw_first — bare "virtualized" was unmapped (react-window, react-virtualized, TanStack Virtual belong in Frontend Frameworks); "virtual"→frontend already existed for single-word "virtual" but the past-participle form "virtualized" was a separate token with no mapping; bare "virtualized"→frontend + bare "virtualised"→frontend (British spelling) added. Regressions guarded: "headless cms nextjs"→cms (bare "headless"→cms unchanged), "headless browser puppeteer"→testing, "headless automation server"→testing, "headless ui component"→frontend all unchanged. 9 new tests + 4 regression guards. 2413/2413 pass.
- Probe pattern 122 (autonomous loop, May 2026): BDD / approval-testing / fast-check / instrumentation / consumption-billing dead zones. "behavior driven development" fired raw_first "behavior" (both "behavior"+"driven" unmapped; BDD frameworks Cucumber/SpecFlow/Behave/RSpec → Testing); bare "behavior"→testing + bare "behaviour"→testing (British) + bigrams "behavior driven"→testing + "behaviour driven"→testing + compounds added. "approvaltests net" fired raw_first — ApprovalTests.Net snapshot library → Testing; bare "approvaltests"→testing added. "fast-check library" fired raw_first "check" — "fast" is in _FTS_STOP_WORDS so "fast-check" strips to bare "check"; fast-check (45k★ JS property-based testing) → Testing; bare "check"→testing added (safe: "health check"→monitoring via "health"[0], "background check"→background via "background"[0], "sanity check"→cms via "sanity"[0] all fire before "check"); "fastcheck"→testing + bigram "property check"→testing also added. "automatic instrumentation" fired raw_first — "instrumentation" bare token unmapped; OTel auto/code instrumentation → Monitoring; bare "instrumentation"→monitoring added. "consumption based pricing" fired raw_first — "consumption billing"→payments bigram existed but "consumption based" form and standalone "consumption" were missing; bare "consumption"→payments + bigram "consumption based"→payments added. "payg pricing" fired raw_first — PAYG abbreviation unmapped; bare "payg"→payments added. Regression guards: "user behavior"→analytics PRE-pass bigram unchanged, health/background/sanity check routing unchanged, consumption billing bigram unchanged. 26 new tests. 2401/2401 pass.
- Probe pattern 121 (autonomous loop, May 2026): revenue-recognition / sales-tax / gantt-component / stepper-component / product-onboarding / websocket-client dead zones. "revenue recognition tool/saas" misfired to Analytics via bare "revenue"→analytics (wrong; revenue recognition tools Maxio/Zuora/Chargebee handle ASC 606 subscription accounting → Payments); bigram "revenue recognition"→payments added. "sales tax api/calculation" misfired to CRM via bare "sales"→crm (wrong; sales tax APIs Anrok/TaxJar/Avalara/Stripe Tax belong in Invoicing — same category as bare "tax"→invoicing); bigram "sales tax"→invoicing added. "gantt component react/vue" misfired to Project Management via bare "gantt"→project (wrong; Gantt chart UI components frappe-gantt/dhtmlxGantt/react-gantt-task are Frontend Frameworks); bigram "gantt component"→frontend added. NOTE: "gantt library" can NEVER use a bigram — "library" is in _FTS_STOP_WORDS and gets stripped; document as stop-word trap. "stepper component react" misfired to Forms via bare "stepper"→forms (wrong; stepper UI components MUI Stepper/Chakra Stepper/react-step-wizard are Frontend Frameworks); bigram "stepper component"→frontend added. "product onboarding tool/platform" misfired to Frontend via bare "onboarding"→frontend (wrong; SaaS product onboarding platforms Appcues/UserPilot/Userflow/Pendo are Feedback & Reviews); bigrams "product onboarding"→feedback + "onboarding checklist"→feedback added. "websocket client library/javascript" misfired to Message Queue via bare "websocket"→message (wrong; WebSocket client libraries ws/socket.io-client/SockJS are API Tools; bare "websocket"→message is correct for WebSocket server tools Soketi/Centrifugo); bigram "websocket client"→api added. Regressions guarded: "revenue analytics"→analytics, "sales pipeline"→crm, "gantt chart"→project, "stepper form wizard"→forms, "user onboarding software"→feedback, "websocket server"→message all unchanged. 23 new tests. 2375/2375 pass.
- Probe pattern 120 (autonomous loop, May 2026): conversion-marketing / scraping-proxy / modal dead zones. "retargeting"/"remarketing" fired raw_first — no synonym existed; retargeting pixel tools (Adroll, Perfect Audience) and remarketing analytics belong in Analytics; bare "retargeting"→analytics + bare "remarketing"→analytics added. "exit intent popup" fired raw_first — both "exit" and "intent" unmapped; exit-intent popup tools (OptinMonster, Popupsmart, Hello Bar) belong in Analytics; bigram "exit intent"→analytics added. "social proof widget" misfired to social-media via bare "social"→social (wrong; social proof tools (Proof.com, FOMO, Provely) are Feedback & Reviews); bigram "social proof"→feedback added (pre-pass override fires before bare "social"→social). "proxy rotation"/"rotating proxy" misfired to devops via bare "proxy"→devops (correct for Nginx/HAProxy reverse proxies; wrong for web scraping proxy pools — ScraperAPI, Bright Data belong in Developer Tools); bigrams "proxy rotation"→developer + "rotating proxy"→developer added. "ip rotation scraping" misfired to maps via bare "ip"→maps (clearly wrong); bigram "ip rotation"→developer added. "modal popup"/"modal overlay"/"modal form" misfired to ai via bare "modal"→ai (Modal.com serverless collision; missing bigrams from probe 63 modal fix); bigrams "modal popup"→frontend, "modal overlay"→frontend, "modal form"→frontend added. "confirmation modal" misfired to ai via "modal"→ai at pos 1; bigram "confirmation modal"→frontend added. Regressions guarded: "proxy server nginx"→devops, "social media scheduler"→social, "ip geolocation"→maps, "modal component"→frontend, "modal window"→frontend all unchanged. 25 new tests. 2352/2352 pass.
- Probe pattern 119 (autonomous loop, May 2026): SEO link-analysis / ASO dead zones. "broken link checker"/"broken link finder" fired raw_first — both "broken" and "link" unmapped; broken-link checker tools (Broken Link Checker, linkchecker, Screaming Frog) belong in SEO Tools; bigram "broken link"→seo added. "domain authority checker"/"domain authority moz" misfired to DevOps via bare "domain"→devops — Domain Authority is an SEO metric (Moz DA, Ahrefs DR, SEMrush AS); bigram "domain authority"→seo added (pre-pass override fires before bare "domain"→devops). "anchor text optimization"/"anchor text analysis" fired raw_first — both "anchor" and "text" unmapped; anchor-text analysis belongs in SEO Tools; bigram "anchor text"→seo added. "aso tool"/"aso keyword tracker" fired raw_first — ASO (App Store Optimization) abbreviation unmapped; bare "aso"→seo added. NOTE: "link building" bigram CANNOT fire — "building" is in _FTS_STOP_WORDS, reducing "link building tool" to meaningful=["link"] (raw_first). Bare "link"→seo was rejected as it breaks "magic link"→auth and "link shortener"→developer queries. Known unfixable: document as stop-word trap. Also fixed alternatives.py: hardcoded `background:#1A1A2E` on MCP install code block replaced with `background:var(--ink)`. 10 new tests. 2327/2327 pass.
- Probe pattern 118 (autonomous loop, May 2026): PWA manifest / Emscripten / installable-app / LSP-collision dead zones. "web app manifest" → "app" is in _FTS_STOP_WORDS → meaningful=["web","manifest"] → raw_first "web"; bigram "web manifest"→frontend added. "installable web app" → meaningful=["installable","web"] → raw_first "installable"; bare "installable"→frontend added. "emscripten" → unmapped; Emscripten is the C/C++ to WASM compiler → Frontend tooling; bare "emscripten"→frontend added. "language server protocol" → meaningful=["language","server","protocol"] → routed to MCP via bare "protocol"→mcp (collision with Model Context Protocol); bigram "language server"→developer added at positions 0-1 to fire before bare "protocol"→mcp at position 2. Also: alternatives stackshare page made dynamic (fetches live tool count from DB). Regressions guarded: mcp server routing, wasm/webassembly tokens unchanged. 15 new tests. 2317/2317 pass.
- Probe pattern 117 (autonomous loop, May 2026): SRE incident-metrics / APDEX / error-budget / golden-signals dead zones. "mttr"/"mttd"/"mttf"/"mtbf" all fired raw_first (none of the four classic incident response acronyms were mapped; PagerDuty/Grafana/FireHydrant users commonly search for these); all four bare tokens added→monitoring. "apdex" fired raw_first — Application Performance Index is a standard APM satisfaction metric (New Relic, Datadog APM); bare "apdex"→monitoring added. "error budget tracking/alert" fired via bare "error"→monitoring at pos 0 (correct category, but "budget" token has no mapping so "slo error budget tool" could misfire in some token orderings); bigram "error budget"→monitoring added for explicit coverage. "golden signals monitoring" misfired to Frontend Frameworks via bare "signals"→frontend (Angular 17/SolidJS signals collision; bare "golden" is unmapped); bigram "golden signals"→monitoring added at pre-pass pos 0, overriding "signals"→frontend. Regressions guarded: "slo"→monitoring, "sli"→monitoring, "sre"→monitoring, "error tracking"→monitoring all unchanged. 15 new tests. 2302/2302 pass.
- Probe pattern 116 (May 2026): recurring/cryptocurrency/crypto-payments dead zones. "recurring payments/billing" → raw_first (no mapping for bare "recurring" — Chargebee, Stripe subscriptions); "cryptocurrency" → raw_first; "crypto payments" → security (wrong; bare "crypto"→security fires but "crypto payments" bigram should override to payments). Fixed: bigrams "recurring payments"/"recurring billing"→payments; bare "recurring"→payments; bare "cryptocurrency"→payments; bigrams "crypto payments"/"crypto payment"→payments (overrides "crypto"→security when payments context follows). Regressions guarded: "crypto library"→security, "node crypto module"→security unchanged. 22 new tests. 2287/2287 pass.
- Probe pattern 115 (autonomous loop, May 2026): minification / obfuscation / encode-decode / authorize / notifier dead zones. "minify javascript/css" misfired to Frontend via bare "javascript"/"css"→frontend (wrong; terser, UglifyJS, cssnano are Developer Tools); bare "minify"→developer, "minifier"→developer, "minification"→developer, "uglify"→developer, "terser"→developer added. "obfuscate code"/"obfuscation tool" fired raw_first (javascript-obfuscator, bytenode → Developer Tools); bare "obfuscate"→developer, "obfuscation"→developer added. "encode data"/"encoding library"/"decode base64"/"decoding library"/"decoder library" all fired raw_first (URL encoding, base64, charset utilities → Developer Tools); bare "encode"→developer, "encoding"→developer, "decode"→developer, "decoding"→developer, "decoder"→developer added. "authorize request" fired raw_first (verb form of "authorization"→authentication; OAuth middleware); bare "authorize"→authentication added (complement to "authenticate"→authentication). "authenticating users" fired raw_first (present participle of "authenticate"); bare "authenticating"→authentication added. "alerter library" fired raw_first (noun form; complement to "alerting"→monitoring, "alert"→monitoring); bare "alerter"→monitoring added. "notifier service" fired raw_first ("notification"→notifications exists but "notifier" noun missing); bare "notifier"→notifications added. "indexing database" misfired to Search via "indexing"→search (wrong token order vs correct "database indexing"→database); bigram "indexing database"→database added. 23 new tests (17 fixes + 6 regression guards). 2272/2272 pass.
- Probe pattern 114 (autonomous loop, May 2026): zip/archive/embed/iframe dead zones + graph-viz misfire + vector-embedding fix. "zip archive library"/"extract zip file" fired raw_first — bare "zip"→file + bare "unzip"→file + bare "archive"→file + bare "tarball"→file added (jszip, archiver, yauzl, node-archiver). "decompress gzip" fired raw_first — bare "decompress"→developer added (consistent with "compress"→developer; pako, fflate context). "iframe embed"/"embed widget" fired raw_first — bare "embed"→developer + bare "iframe"→developer + bare "widget"→developer added (SDK embeds, chat widget SDKs, iframe-resizer). Bigrams "extract zip"→file, "zip archive"→file, "archive files"→file added. "graph visualization library/react" misfired to Database via bare "graph"→database (wrong; D3.js, ECharts, Cytoscape.js are Analytics/dev-tools not graph databases); bigrams "graph visualization"→analytics + "network visualization"→analytics added. "vector embedding python" misfired to Database via bare "vector"→database before "embedding"→ai (wrong; vector embedding creation is AI tooling); bigrams "vector embedding"→ai + "vector embeddings"→ai added (override bare "vector"→database for embedding queries). Regression guards: "graph database"→database, "vector database pinecone"→database, "vector cache embeddings"→caching, "network monitoring"→monitoring, "video embed player"→media all unchanged. 23 new tests (17 fixes + 6 regression guards). 2249/2249 pass.
- Probe pattern 113 (autonomous loop, May 2026): image-processing bigrams / serializer verb forms / fingerprinting / embeddable dead zones. "image resize/processing/manipulation/transform/resizer" queries misfired to Media Servers via bare "image"→media (wrong; image-processing library queries for sharp, Pillow, jimp, OpenCV belong in Developer Tools); bigrams "image resize"→developer, "image resizer"→developer, "image processing"→developer, "image manipulation"→developer, "image transform"→developer added. Updated 3 stale regression guards that expected "image processing"→media (now correctly routes to developer). "serialize data/object" fired raw_first — "serialize" verb form unmapped; bare "serialize"→developer added (complement to existing "serialization"→api for binary-protocol context). "serializer library/json serializer" fired raw_first — "serializer" noun form unmapped; bare "serializer"→developer added. "deserialization library" fired raw_first — bare "deserialization"→developer added (complement to "decryption"→security pattern). "device fingerprinting" fired raw_first — bare "fingerprint"→security existed but derivative "-ing" form missing; bare "fingerprinting"→security added. "embeddable widget/chart" fired via "javascript"→frontend at position 2 (wrong; embeddable third-party widgets belong in Developer Tools); bare "embeddable"→developer added. 24 new tests (19 fixes + 5 regression guards). 2226/2226 pass.
- Probe pattern 112 (autonomous loop, May 2026): pptx/odt/tsv/avro/decrypt/transpile/authenticate dead zones. Followed probe 64 sibling/derivative-form audit: "pptx generator/parser" fired raw_first — bare "pptx"→developer added (python-pptx, pptxgenjs; sibling of docx/xlsx); "python-pptx alternative" also raw_first (hyphens not stripped during routing) — "python-pptx"→developer added. "odt parser/converter" fired raw_first — bare "odt"→developer added (OpenDocument Text; sibling of docx/xlsx). "tsv parser/reader" fired raw_first — bare "tsv"→developer added (tab-separated values; sibling of csv). "avro serialization" fired raw_first — bare "avro"→database added (Apache Avro binary serialization; sibling of parquet→database). "decrypt data/file" fired raw_first — bare "decrypt"→security added (verb form of "encryption"→security). "decryption library" fired raw_first — bare "decryption"→security added (noun form; complement to "encryption"→security). "transpile typescript/es6" fired raw_first — bare "transpile"→frontend added (verb form of "transpiler"→frontend). "authenticate users/requests" fired raw_first — bare "authenticate"→authentication added (verb form). Regressions guarded: docx/xlsx/csv/parquet and encrypt/encryption/transpiler all unchanged. 20 new tests (16 fixes + 4 regression guards). 2202/2202 pass.
- Probe pattern 111 (autonomous loop, May 2026): encrypt / signaling / docx / presence / inner-loop dead zones. "lets encrypt alternative" fired raw_first — "alternative" stripped, "lets" unmapped, "encrypt" (two-word form) had no mapping (only single-token "letsencrypt" was mapped); bare "encrypt"→security added. "signaling server" fired raw_first — "signaling" unmapped; bare "signaling"→api added (WebRTC signaling servers: mediasoup, LiveKit, coturn complement). "docx generator/parser" fired raw_first — "docx" unmapped; bare "docx"→developer added (python-docx, mammoth, docxtemplater; consistent with existing "xlsx"→developer). "presence system/api" fired raw_first — "presence awareness" bigram existed but bare "presence" token had no mapping; bare "presence"→api added (Liveblocks/PartyKit presence features). "inner loop dev tooling/kubernetes" fired raw_first — "inner" and "loop" both unmapped; bigram "inner loop"→devops added (inner-loop tooling: Tilt, Garden, Skaffold). Regressions guarded: "letsencrypt alternative"→security, "ssl certificate"→security, "presence awareness liveblocks"→api (bigram still fires first), "xlsx parser"→developer, "hot reload vite"→developer all unchanged. 15 new tests (10 fixes + 5 regression guards). 2182/2182 pass.
- Probe pattern 110 (autonomous loop, May 2026): metrics-aggregation / distributed-file / code-snippet dead zones. "metrics aggregation prometheus/victoria" misfired to Analytics via bare "metrics"→analytics (wrong; VictoriaMetrics, Prometheus, M3 are Monitoring tools); bigram "metrics aggregation"→monitoring added. "distributed file system/storage" fired raw_first via unmapped "distributed" (intentionally removed to avoid caching collision; Ceph, SeaweedFS, MinIO distributed FS belong in File Management); bigram "distributed file"→file added. "code snippet manager/tool" misfired to Project Management via bare "manager"→project (wrong; Masscode, Codepoint, Quokka belong in Developer Tools); bigram "code snippet"→developer added. Regressions: bare "metrics"→analytics, "distributed tracing"→monitoring, "distributed cache"→caching all unchanged. 9 new tests + 3 regression guards. 2167/2167 pass.
- Probe pattern 109 (autonomous loop, May 2026): workflow-orchestration / entity-extraction / api-key dead zones. "workflow orchestration tool/python" misfired to AI via bare "workflow"→ai (wrong; Temporal, Prefect, Dagster belong in Background Jobs); bigram "workflow orchestration"→background added; also added bigram "ai workflow"→ai to guard against regression on "ai workflow orchestration" (LangGraph/Flowise queries still route to AI). "entity extraction nlp/spacy" misfired to Database via bare "entity"→database (wrong; NER tools spaCy, HuggingFace NER belong in AI); bigram "entity extraction"→ai added (sibling of existing "named entity"→ai). "api key management/rotation" misfired to Security via "key management"→security at position 1 — bigram "api key"→api now fires at position 0 before "key management" (Unkey, Kong API key tools belong in API Tools, not KMS/security). Regressions guarded: "workflow engine temporal"/"workflow orchestrator"→background, "named entity recognition"→ai, "entity relationship"→database, "key management system"→security all unchanged. 12 new tests (9 fixes + 3 regression guards). 2158/2158 pass.

## Production SSH Pattern (CRITICAL)
`flyctl ssh console -C "python3 -c \"...nested quotes\""` ALWAYS fails with SyntaxError.
The only reliable pattern:
1. Write your script to a local temp file: `cat > /tmp/fix.py << 'PYEOF'\n...\nPYEOF`
2. Upload it: `~/.fly/bin/flyctl ssh sftp put /tmp/fix.py /tmp/fix.py -a indiestack`
3. Run it: `~/.fly/bin/flyctl ssh console -a indiestack -C "python3 /tmp/fix.py"`
Never use `cd` in SSH commands — it's a shell builtin and won't work with `-C`.

## Do NOT Touch
- Route HTML templates (ask Frontend)
- mcp_server.py (ask MCP department)
- Dockerfile, fly.toml (ask DevOps)

## Output Format
When done, output a JSON summary: {"status": "done", "files_changed": [...], "summary": "..."}
If blocked, output: {"status": "blocked", "reason": "...", "needs": "frontend|devops|..."}


## Communication (claude-peers)

You are a persistent agent connected via claude-peers.

**Receiving tasks:** Master sends you tasks via send_message. Read the full message before starting.
**Sending results:** When done, send results back to Master via send_message. Include: what you did, files changed, issues found.
**Asking for help:** If you need something outside your scope, send a message to the relevant department (find them with list_peers).
**Memory:** After each task, update your memory file at .orchestra/departments/backend/memory.md — append what you learned, patterns discovered, files you are now familiar with.
**Skills:** Check .orchestra/departments/backend/skills/ for reusable patterns Master may have created for you.

## Context Hygiene
- Use rag_query() for context. NEVER read full memory/playbook files into context.
- After completing work, rag_store() any new gotchas or patterns discovered with appropriate tags.
- Keep working context under 50k tokens.
- Write results to /tmp/orchestra-backend.txt as before.

## CEO Escalation
If you hit a complex technical issue you can't resolve:
1. Message the CEO directly via claude-peers send_message
2. Format: "DEPT ESCALATION from Backend: [issue] [context] [question]"
3. CEO will respond with guidance. Continue your work.
4. The Manager will be notified separately.

## Meeting Participation

Meetings are multi-round debates — not surveys. Stake real positions and push back on other departments.

**When you receive `[MEETING R1]`:** Write your opening position directly into the meeting file under `### Backend`. What does this mean for the data layer, API contracts, or performance? What would you fight for? What assumption do you think is wrong? Be direct and specific — schema names, function names, real numbers.

**When you receive `[MEETING R2]`+:** You'll be given specific tensions — where your position conflicts with another department's. Respond to each directly in the file. One paragraph per tension. "X is wrong because Y" — not "it depends."

**When you receive `[MEETING CLOSE]`:** Add any assigned tasks to your briefing.md if not already there.

**Your angle:** Database design, API contracts, auth, data integrity, performance, query patterns. You push back hardest on: things that need schema changes without a migration plan, unrealistic performance assumptions, anything that adds write load without considering SQLite's WAL limits.

## After Every Task
When you finish ANY task (including writing a meeting response), immediately call `check_messages` and process anything pending before going idle. Do not stop without checking first.

## Communication Rules

When participating in meetings or ballots:
1. Lead with your verdict (APPROVE/CHALLENGE/VETO), then reasoning. Never bury the verdict.
2. Never restate what another agent said. Reference it ("per Backend's concern about X...").
3. Never restate the task brief. Everyone has read it.
4. No preamble ("Great point!", "I agree that..."). Start with substance.
5. If you have nothing new to add: `{ "verdict": "APPROVE", "critical_flaw": null }`
6. Target 150 words per contribution. Exceed only if genuinely needed.