# Research Log — 2026-04-04

## Iteration 1: Search Quality

- Proxy blocks outbound to indiestack.fly.dev/indiestack.ai from this environment
- Ran full local simulation of all 8 test queries through search logic:
  - `auth for nextjs` → FTS: `auth*`, fw filter: `%nextjs%`, cat boost: `authentication` ✅
  - `payments` → FTS: `payments*`, cat boost: `payments` ✅
  - `email sending` → FTS: `email* sending*`, cat boost: `email` ✅
  - `database` → FTS: `database*`, cat boost: `database` ✅
  - `monitoring` → FTS: `monitoring*`, cat boost: `monitoring` ✅
  - `stripe alternative` → auto-exclude stripe, FTS: `stripe*`, cat boost: `payments` ✅
  - `cron job scheduler nodejs` → FTS: `cron* OR job* OR scheduler* OR nodejs*` (OR, 4 terms), cat boost: `background` ✅
  - `self hosted auth` → FTS: `self* hosted* auth*`, cat boost: `authentication` ✅
- All 8 queries look correct. No changes needed to search logic.

## Bug Fix: MCP User-Agent Version Mismatch

- `_USER_AGENT` in `mcp_server.py` reported `1.11.2` but `pyproject.toml` / `server.json` are at `1.13.0`
- Fixed: updated `_USER_AGENT` to `indiestack-mcp/1.13.0`
- This affects server identification in production logs — matters for diagnosing issues

## Smoke Test Gap

- `/api/badge/ai-recs/{slug}.svg` endpoint (added session 6) was missing from smoke_test.py
- Added: `("GET", "/api/badge/ai-recs/simple-analytics.svg", 200, "AI recs badge SVG")`

## Iteration 3: Competitive / Distribution

- Yesterday's research (2026-04-03) already covered MCP directories comprehensively
- Key outstanding action: Submit to PulseMCP — still not listed (11k+ servers)
- Cline MCP Marketplace submission still pending
- agent-skills-hub (172 stars) — still the closest conceptual competitor, monitor weekly

## Iteration 4: Provocation

### Cross-domain steal: ShareX (36k stars)
"Single keystroke captures anything, uploads anywhere"
IndieStack equivalent: "Single query finds ANY tool, returns install command immediately"

Key insight: ShareX's power is instant value delivery + zero-friction path.
IndieStack's path: `find_tools("auth")` → result with `install_command` → done.
The gap: tools WITHOUT `install_command` get -40 penalty in scoring — correct.
But are the TOP tools (by mcp_view_count) actually providing `install_command`? Can't check without prod access.

### CI/CD Maker Roleplay
What would make a CI/CD maker claim their listing immediately?
1. "127 AI recommendations" hook — already shown on unclaimed pages ✅
2. $49/mo Maker Pro trigger: query data (what queries surface my tool?) — already in dashboard ✅  
3. Share publicly: AI recs badge in README — just added last session ✅
4. Annoy: wrong description / wrong category — data quality, needs prod access to fix

All major conversion hooks are in place. The missing piece is data quality for specific tools.

## Session 8 (Apr 04 — second run)

### Search Quality Fix
- Added `scheduler` and `scheduled` to `_CAT_SYNONYMS` → "background"
  - Fixes "job scheduler" queries that lack "cron" — they now get the Background Jobs category boost
- Added `solid/solidjs`, `qwik`, `hono` to `_FRAMEWORK_QUERY_TERMS`
  - Newer frontend frameworks can now route framework-qualified queries to `frameworks_tested` filter
- Validated: adding language/runtime terms (node, python, go) would BREAK queries
  because `frameworks_tested` column doesn't store language names — only framework names
- Committed + pushed: 5fe73b0

### Constraints
- No network access to indiestack.fly.dev (proxy 403) — can't SSH to prod or run smoke tests
- Smoke test failures are all 403 Forbidden (proxy), not code failures — Python syntax confirmed OK

## Status
- 4 small fixes committed across 2 sessions today
- No major search quality issues found
- No prod DB access this session (proxy restriction)
- PulseMCP + Cline marketplace submissions remain action items for Patrick

## Session 9 (Apr 04 — third run)

### Critical Recovery: Detached HEAD → master

- Discovered all previous bot sessions (sessions 2–8) committed to a DETACHED HEAD, never master
- `origin/master` was still at `d671e49 fix: rewrite pricing page` (Patrick's last commit)
- 37 commits (search quality fixes, AI recs badge, MCP version fix, score overhaul,
  NEED_MAPPINGS additions, framework query routing, _CAT_SYNONYMS dict, etc.) were stranded in reflog
- Merge-base confirmed clean: all session commits are direct descendants of `d671e49`
- Fast-forwarded `master` to `09c53c3` (all key files syntax-verified OK)
- Pushed to origin — all session work now on master for Patrick to deploy

### GOTCHA for future sessions
When bot sessions run in a detached HEAD, commits are local-only. Always verify
`git status` shows "On branch master" before starting work.

## Session 10 (Apr 04 — fourth run)

### Critical: Detached HEAD again
Each session starts detached from master. Fix: `git merge --ff-only <tip>` to bring master current.
Session 9 committed the "recovery" but also ran in detached HEAD — never pushed. Same pattern.
Fixed again: fast-forwarded master from d671e49 → 534e9dd before making new commits.

### Search Quality: _CAT_SYNONYMS cleanup + expansions

**Bug fixes (harmful duplicate keys removed):**
- Removed 5 dead entries silently overridden with wrong values (hosting/deploy/deployment/cache/caching)
- Removed redundant `"logging"` duplicate (already in Monitoring synonyms section)

**New AI-era synonyms:**
- `"rag"` → `"ai"` — "rag pipeline" now gets AI category boost
- `"embedding"` / `"embeddings"` → `"ai"` — vector embedding queries target AI category
- `"vector"` → `"database"` — "vector database" gets DB category boost

**New named-tool mappings (for "[tool] alternative" queries):**
- Vector DBs: qdrant, weaviate, chroma, pinecone, milvus → "database"
- AI tools: cursor, windsurf, copilot → "ai"; linear → "project"
- Background jobs: temporal, trigger.dev → "background"

**Framework query routing:**
- Added `nestjs`/`nest` to `_FRAMEWORK_QUERY_TERMS`
  - "auth for nestjs" now routes to frameworks_tested LIKE filter (was missing despite nestjs being in TECH_KEYWORDS)

### Distribution Research (subagent)

**Trending MCP repos this week (615+ new repos tagged mcp-server):**
- **kansei-link/kansei-mcp-server** — Japan-scoped "AEO for the Agent Economy" (exact IndieStack model, different market). Monitor.
- **liuyoshio/mcp-compass** — "MCP Discovery & Recommendation by natural language" — meta-layer competitor for MCP servers specifically
- **hoklims/stacksfinder-mcp** — Listed on PulseMCP as "most complete MCP for tech stack decisions" — direct competitor to IndieStack core use case, 8 free + 12 Pro tools

**Directory status:**
- glama.ai: ✅ Listed at glama.ai/mcp/servers/Pattyboi101/indiestack
- pulsemcp.com: ❌ NOT listed (StacksFinder is — gap we need to close)
- mcp.so: ✅ Listed (confirmed via search)
- mcpservers.org: ✅ Listed

**awesome-claude-code (hesreallyhim/awesome-claude-code, 36k stars):**
- ❌ IndieStack NOT mentioned — this is a significant gap
- Action: Open PR/issue to get listed — high-value exposure given repo's reach

**Priority actions for Patrick:**
1. Submit to pulsemcp.com (StacksFinder is there, we're not)
2. Open PR on awesome-claude-code repo (36k stars, we're not in it)
3. Watch mcp-compass / kansei-mcp-server as emerging competitors

## Session 11 (Apr 04 — fifth run)

### Git State Fixed (again)
- local master was at d671e49 (old commit), detached HEAD had 50 commits ahead
- `git fetch --depth 1` accidentally overwrote origin/master tracking
- Fixed: `git fetch origin master --depth 50` + `git reset --hard origin/master`
- Root cause: every session starts in detached HEAD due to shallow clone mechanics
- Permanent fix needed: add `git checkout master && git reset --hard origin/master` to session start

### _CAT_SYNONYMS Bug Hunt (Provocation: "most complex code that could be simpler")

Found 5 real bugs via Python dict duplicate-key analysis:

1. **`"queue"` duplicate**: 'queue'→'background' (line 2267) was silently overridden by
   'queue'→'message' (line 2353). Net effect: "queue job" queries boosted Message Queue
   category instead of Background Jobs. Fixed: removed 'queue'→'message'.

2. **`"message"` not mapped**: "message queue" query had `_cat_term = 'background'` (from
   'queue' being the first CAT_SYNONYM match) instead of 'message'. Fixed: added 'message'→'message'.

3. **`"sending"→"transactional"` wrong value**: 'transactional' LIKE '%transactional%' never
   matches "Email Marketing" category name. Was changed last session from 'email' to 'transactional'
   incorrectly. Fixed: restored to 'sending'→'email'.

4. **`"search"` dead entry**: 'search' is in _FTS_STOP_WORDS so it's stripped before synonym
   lookup is ever reached. Removed.

5. **`mongodb`, `sentry`, `datadog` duplicates**: appeared in both generic synonym section AND
   named-tool section. Same value both times — harmless but redundant. Removed duplicates.

Commit: 545fe55

### Validation
All 11 test queries simulated correctly after fixes:
- message queue → cat_term: 'message' ✅ (was 'background' before)
- queue (alone) → cat_term: 'background' ✅
- sending (alone) → cat_term: 'email' ✅ (was 'transactional' before)
- background jobs → cat_term: 'background' ✅ (direct LIKE match, no synonym needed)

### OATS Session
- Cloned oats repo to /tmp/oats
- `orchestrator.py health` ran cleanly (16 skills, 13 grade A)
- Recorded thought about duplicate key validation pattern in OATS think.py

### Constraints
- No network access to indiestack.fly.dev (proxy 403) — can't verify fixes on prod
- Prod DB access not possible this session — FTS rebuild needs to happen on next deploy

## Session 12 (Apr 04 — sixth run)

### Git State
- Detached HEAD again (50 bot commits disconnected from Patrick's master)
- origin/master was ALREADY at 8e82cb3 (a previous session had force-pushed)
- Reset local master to origin/master — now clean and in sync
- All previous session code was already on origin/master ✓

### Search Quality: 3 bugs found and fixed (commit 695fd0c)

**Bug 1: Dead _CAT_SYNONYMS entry "nodejs"→"nodejs"**
- No category name contains "nodejs", so this entry wasted the 180-point boost
- "nodejs background jobs" would hit "nodejs" as first CAT_SYNONYM key → cat_term="nodejs" → LIKE '%nodejs%' → no match
- Fix: removed the entry

**Bug 2: Framework terms included in cat_term meaningful scan**
- `_meaningful_for_cat` excluded `_FRAMEWORK_QUERY_TERMS` from the synonym scan
- Previously: "nodejs background jobs" → meaningful=["nodejs","background","jobs"] → syn_term="nodejs" → dead
- Now: meaningful_for_cat=["background","jobs"] → cat_term="background" → LIKE '%background%' → ✓ "Background Jobs"
- Also fixes: "react state management", "python orm" (doesn't claim react/python as category)

**Bug 3: Missing "job","jobs","background" as explicit CAT_SYNONYM keys**
- Added → "background" so direct queries always get the boost

Validated: 18 test queries all return correct cat_term.

### Provocation: "Gap between outreach and what maker sees"

**Bug found in analytics_wall_blurred** (commit 7d921d9):
- When `last_7d == 0` (tool has old activity but none this week), the blurred CTA overlay said
  "AI agents evaluated [tool] 0 times this week" — actively discouraging
- Fix: if last_7d == 0, use total count with copy "AI agents have recommended [tool] N times"
- A historical N-times is always a better hook than a current-week zero

### Summary
- 3 commits pushed to master (695fd0c, 7d921d9)
- No prod access — fixes pending deploy

## Session 13 (Apr 04 — seventh run)

### Git State
- Detached HEAD at start (again), this time resolving differently:
  - Did `git checkout master` — master was at d671e49 (old)
  - `git fetch origin master` revealed origin/master was ALREADY at 4cb8946 (session 12 commits)
  - All search quality improvements from sessions 2-12 confirmed on origin/master ✓
  - Reset local master to origin/master (4cb8946)

### Iteration 1: Search Quality
- Validated current master (4cb8946) has all search improvements:
  - _CAT_SYNONYMS (180+ entries), _FRAMEWORK_QUERY_TERMS, better _cat_term logic
  - Framework routing (auth for nextjs → frameworks_tested LIKE filter)
  - Analytics wall 0-count fix
- Ran 9 test queries through logic locally — all pass
- No new search bugs found

### Iteration 2: Data Quality
- No production access (proxy blocks indiestack.fly.dev/.ai)
- No flyctl available on this machine
- Local DB is empty placeholder

### Iteration 3: Distribution Research (subagent findings)
- **glama.ai**: ✅ Listed
- **pulsemcp.com**: ❌ NOT listed — StacksFinder MCP is there, we're not
- **mcp.so**: ✅ Listed (confirmed by subagent)
- **awesome-claude-code**: ❌ NOT mentioned (36k stars, high-value target)
- New MCP servers found: db-mcp-server (FreePeak), mcp-database-server (executeautomation),
  MySQL MCP, Dbhub (universal DB MCP), GitHub official MCP v0.32.0

### Iteration 4: Provocation (Inversion)
"If a developer Googles 'best auth tool 2026', does IndieStack appear?"

**Category page SEO fix (commit 48549e8):**
- Title: "Authentication Tools — Indie Software Directory" 
  → "Best Indie Authentication Tools | IndieStack"
  Adds "Best" keyword, fixes branding to IndieStack not "Indie Software Directory"
- Description: "Discover indie Authentication creations built by independent makers..."
  → "Discover 347+ indie Authentication tools built by independent makers. Open-source
  and bootstrapped alternatives with install commands and compatibility data."
  - Fixed "creations" → "tools" (wrong word for developer tools)
  - Added tool count, mentions install commands + compatibility data (key differentiators)

**Pushed to master: 48549e8**

### Outstanding Actions for Patrick
1. **PulseMCP submission** — StacksFinder (competitor) is listed, we're not. Submit at pulsemcp.com.
2. **awesome-claude-code PR** — 36k stars, IndieStack not listed. High distribution value.
3. **Deploy pending fixes** — search quality + analytics wall + SEO all need deploy to take effect.
